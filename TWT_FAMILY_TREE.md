# THE TWT FAMILY TREE — the pick register in tree form

*Instituted by RUL-048 (2026-08-18, P-2 ratified as re-shaped by the human coordinator).
The coordinator's diagnosis, verbatim in substance: "the rulings were always presented to me
but often in a language I couldn't make sense of. The problem was that the theory didn't have
a core family and candidates. It just had one evolving version." This file is the fix's
architecture: the family has a ROOT (the Core), PREFERRED DIRECTIONS (the endorsements), and
BRANCHES (the picks) — so a branch point is VISIBLE as a branch point the moment it presents
itself, and no load-bearing pick is ever silent again.*

## Maintenance rule (RUL-048; canon §2 commitment-budget bullet)

A load-bearing pick enters this tree IN THE SAME PASS it is made: its node records the MENU
it was picked from, WHY (the named result that needs it), the REVERT CLAUSE (what un-picks
it and what falls), and WHAT RIDES it. CORE-touching picks require the coordinator's
plain-language sign-off BEFORE the pick; all others proceed on the recorded node. Stamps
per RUL-047's vocabulary: CORE / ENDORSED / PINNED / HELD / REFUSED.

---

## ROOT — THE CORE (family-defining; RUL-047 final tally, 2026-08-18)

| Axiom | Statement |
|---|---|
| S1a | Reality is a 4D Euclidean material substrate |
| S2 | Meta-time τ₅ with `e₅² = −1` — the necessary ontological move (movement of information) |
| S3 | The advancing wavefront + lock `x₄ = c·τ`; observers are slices; a preferred foliation exists |
| S4 | Matter = defect — a protected pattern of the medium, not a substance in it |
| S5 | The medium is DRIVEN — one-way advance is constitutive |
| B-6 | The preferred foliation IS the cosmic (CMB) rest frame — **the family's kill condition** |
| — | **REFUSED: the field-as-ontology picture.** The substrate is a material medium; the rotor-field formalism is instance-level description. **Gloss (RUL-058):** a field is refused only as a *fundamental description of what the world is made of* — never as a mathematical description of an emergent property of the medium (as temperature is a field-description of molecular motion); the grain→cell map is a REAL PHYSICAL RELATION, plausibly wave-driven, not a bookkeeping device; the cell's initial description is "an emergent pattern" |

### Core nodes — the local state space (added 2026-08-18, RUL-057; keeper record `knowledge/audit/pivot_2026-08-17/KEEPER_SPIN34_ADJUDICATION_2026-08-18.md`)

| Node | Status | Menu | Resolution / branches | Revert clause |
|---|---|---|---|---|
| **LS-6** — the local state's SIZE | **SIGNED (Core amendment, RUL-057):** six real parameters — a 4D orientation at each site, grain layer, inherited unchanged by the continuum field. **WITNESS-FREE at family level (RUL-083, 2026-08-21): the axiom is stated for any grain structure; its existing computational witness (R-102's D4-sited construction + the engine-checked size argument) is RELOCATED TO V3 as that candidate's witness — each family member re-witnesses on its own grain** | {`S²` (2) · `Spin(3)` (3) · 4D-orientation class (6): `Spin(4)`/`SO(4)`} — the three readings found in live use (JD menu §1.2 + the keeper's Collision 1) | 6 is coherence-decided (keeper, four named grounds: no generation/weak carrier in `exp(𝓛)`; ℤ₂ axis ≠ dimension axis; `Cl⁺(3,0)` provenance; asymmetric blast radius). §D.3.2's 3-parameter text = drift, corrected at the repair pass; 2-/3-parameter readings survive as stated L-orbit-sector reductions | Keeper draft-row revert list (restore Spin(3) text; demote R-002/R-052; N_eff → 3, Λ_S → √(4π)M_Pl; withdraw the Layer-1 carriers) |
| **LS-ℤ₂** — WHERE the sign lives | **OPEN BRANCH (deliberately unpicked, RUL-057)** | {sign-in-the-state: `Spin(4)`, one-sided action \| sign-in-the-quantization-sector: `SO(4)` + odd character of π₁ = ℤ₂} | Both branches carry identical π₃ (windings) and π₄ (FR ℤ₂) — covering maps are iso on πₙ≥2; fermionic phenomenology available on both (the genuine fermionic bit is V3-11's FR odd-character pick, branch-blind). Discriminators: **(1) RE-POINTED, NOT READ OUT (★★ J,D/Γ rework).** Within the **frame-bilinear class** every banked bond term is EVEN under `R_i → −R_i` (the vector rep is sign-blind) and the only odd channel, the pseudoscalar `χ`, has allowed dimension **0** at the driven group [48] — so the local sign is gauge *given that class*. But the class itself is the **V3-2 pick, not a measurement**: the rotor-linear class has ODD allowed channels (J:2, D:2) at the same group, and **no banked helix- or quadratic-level object separates the two readings** — the rotor-linear model reproduces the banked closed form and the banked 10.5088° *character-for-character* in its own half-angle variable `φ = q/2` (engine-verified, maxdiff 0.000e+00), and the kinetic-normalisation difference is absorbed because `J` is never independently measured (it is back-fit through `f_π`). The pitch witness that the first draft of this read-out relied on is therefore **VACUOUS and is struck** — a tight tolerance on a check that could not have failed. Discriminator (1) is re-pointed at **the quartic / Skyrme sector**, where the readings provably differ (the rotor-linear J-quartic lies OUTSIDE `span{S₁,S₂}`, relative residual ~0.2–0.5, where the frame-bilinear one is inside at 8.9e-16) [RIDER 2026-09-17, the JD-7 second design review: the ~0.2–0.5 is one sampling-unstable max-norm statistic (0.2181 at probe_07's recorded seed and n = 200; 0.4258 at n = 2000); the stable figure is the L2 fraction ≈ 0.15 across seeds and n — **SUPERSEDED 2026-09-18 by an IDENTITY (JD-7 leg B as re-cut by its result review, `jd7_quartic_2026-09-17/JD7_NOTE_2026-09-17.md` REV 1; banked R-206, `g8_jd7_quartic_pfaffian_channel_identity`): the two readings differ at O(X⁴) by EXACTLY the pseudoscalar (Pfaffian) channel — rotor-linear Σ_b ⟨X_b⁴⟩₀ = 4 S₁ + 2 S₂ + 6 Σ_b Pf(X_b)², frame-bilinear Σ_b Tr(X_b⁴) = 8 S₁ + 4 S₂ (residuals 4e-16); the 0.148 was the size of that fixed channel under one arbitrary Gaussian ensemble (0.003–0.26 across ensembles) and is NOT a constant of the theory — the vacuous-witness class this row struck once already**; the 1/24 reading-invariant (KB1); and the propagation of the O(X⁴) difference to e is BLOCKED at the dressed / non-local sector — e_bare ≈ 0.87 from the bare 1/24, the relation e ≈ √18/(D/J) among dressed couplings, no banked map from the tensor to e; discriminator (1)'s read-out is the S₁/S₂ decomposition under both readings (JD-7 leg B), a located gap for the rest], i.e. at R-107/R-109's `κ_F = J/24` recovery and the ~1% `√18/(D/J)` e-match — **BUILT 2026-09-18, AND NON-DISCRIMINATING ON BOTH RECORDED OBSERVABLE ROUTES**: the bare `κ_F = J/24` is measured **reading-INVARIANT** (KB1), so that route could not have separated the readings and is **retired as a discriminator** (the Ex.9 tell, one level up); and the e-match route is **BLOCKED at the dressed / non-local sector** (no banked map from the O(X⁴) tensor to e; `e_bare ≈ 0.87`). What discriminator (1) carries is therefore a **tensor-level measurement with no observable attached** — a located gap (negatives ledger N62, sub-note JD-7, STATUS 2026-09-18) [CROSS-POINTER RESOLVED 2026-09-18: the quartic sector was also the GATE on the forced-activity recon's only open member — `g8_n4_soliton_scoping_2026-09-02/FORCED_ACTIVITY_C_NOTE_2026-09-17.md` **REV 8** — the one computation ran and returned a class statement for neither question]. Re-openers, restated honestly: (a) a rotor-linear bond term — **excluded by nothing banked** (the old "excluded by the corpus's own banked pitch" is REFUTED); (b) Hamiltonian-level reflection breaking to [24], where `χ:2` opens and is ODD — a source the drive axis alone does not supply. Correspondingly the bond-menu **count is ten in the frame-bilinear class and four in the rotor-linear class**, and the choice between them is the class pick, not an inference. **Independently re-confirmed 2026-08-23** on four pipelines (`bond_invariant_menu_frame_bilinear`; Γ-channel referent closure) — the full ladders are 2/8/10/12 frame-bilinear and 1/2/4/8 rotor-linear across [1152]/[96]/[48]/[24], with `χ` dim 0 at the driven group and 2 at [24]. **No change is owed at this node** — it already carried the fork and its disposition, and the closure round is credited CONFIRMED-ALREADY-RECORDED, not originating. **(2)** ℤ₂ domain-wall / measure–topological sector (Spin branch only, unbuilt) — untouched by the rework and the node's live discriminator; **(3)** emergent-QM odd-sector support at §B.3.5's π₁(Q_N) = ℤ₂ (probe-sized). If none pins it: RUL-051 family freedom — one member, many descriptions. Prior-art credit owed on the relocation picture: sign-even orientation bilinears ⇒ local ℤ₂ gauge structure with the physics relocating to the topological/covering sector is Lammert–Rokhsar–Toner (PRL **70**, 1650 (1993); PRE **52**, 1778 (1995)) | Picking either branch: record here + re-word the amendment's ℤ₂ sentence; the V2-era "one-sided = spinor convention" clause is NOT restored as an input either way (RUL-057 status correction: fermionicity cannot pay for it) |
| **S5-LOCAL** — is the medium's activity LOCAL (each site a non-conservative element) or only common-mode (the carrier's advance)? **[ENDORSED 2026-09-17 20:41 — the coordinator's word on three conditions ("Stamp S5-LOCAL ENDORSED on those conditions"); RUL-135; RUL-084's ground: a preferred direction a family member may go the other way on — NOT an axiom amendment, S5's text unchanged; drafted PROPOSED by the 4b keeper at 20:16 so the stamp landed same-pass (RUL-048(ii))]** | **ENDORSED (RUL-084 class; RUL-135)** — the three conditions: (1) the ground is RUL-084's preference, the recon's finding cited as supporting evidence in its own words (forced within the members computed), never 'the rule fired'; (2) same-pass bookkeeping (this node, the register row, the handoff); (3) the scope is 'the site is itself an active element' — the limit-cycle-engine picture was a CANDIDATE model of activity until a run on the banked kernel earned it — EARNED and ENDORSED 2026-09-22 (RUL-139(a)): P4.1's drive-free world [A] and P4.5's coherent branch exhibit the self-sustained, gain-balanced, phase-coherent rotating non-vacuum state on the banked V4-ACTIVE generator; a preferred direction of this node, not an axiom; every result riding it stays CANDIDATE on V4-ACTIVE. Downstream results carry 'given S5-LOCAL (ENDORSED)', never DERIVED-P. | MENU (S5's two faces, advisory rounds 3–4): {**carrier-only** — S5 as ratified: the one-way advance is constitutive and the site is passive (H9); the defect is then sustained by something else, and nothing computed does it [2026-09-22: on the carrier + site face nothing computed holds a planted winding either — the W1 active flow loses one no later than its passive twin on the vacuum (DEG-2) and the σ = 5 NESS (P4.4 / N76), the loss being the planted shape's own collapse; the node's endorsement rests on RUL-084's preference and the NESS (RUL-139(a)), neither touched] [SUPERSEDED-IN-PART 2026-09-19, the P1.9 ceremony: of the members computed, ONE now does so on the static COST face — at κ_a = 0.4994 the S_a plaquette quartic holds a non-vacuum critical point of E₂ + κS_a carrying degree −1 (|g|₂ 1.19e-3 against |∇E₂| = |κ∇S_a| = 91.9236 at cos −1.000000000, keeper-reproduced), whose localization is undecided at this cell (its tail reaches the periodic images; the box read P1.11 owed) and whose Hessian is unread — an exhibited critical point, not yet a sustained localized defect] \| **carrier + site** — the site is itself active (V4-ACTIVE's counted premise promoted from CANDIDATE to family level)}. GROUND with its conditioning class: local activity is FORCED-WITHIN-THE-MEMBERS-COMPUTED — the bilinear bond-invariant menu (J:2 ⊕ D:2 ⊕ Γ:6, computed exhaustive), the bi-invariant (E2′) and rigid-body kinetic metrics, ONE plaquette-quartic member, the external drive refuted under the driven flow, and member (b) excluded by the LS axiom (a pick) — on the grain lattice at Ω₀ = 0, decided on three faces (static COST, inertial c = 0, driven); the R-198 + R-201 equivalence makes a root of γ_eff the only stationary non-vacuum route on the passive kernel. TIER: CANDIDATE. [GROUND PARTIAL 2026-09-19 (the keeper's R2): 'FORCED-WITHIN-THE-MEMBERS-COMPUTED' held as of 2026-09-17; of the four O(4)-class quartic members since computed, S_a holds a non-vacuum COST-critical point with degree −1 (P1.9), so 'every member computed falls' is no longer true and this node stands on RUL-084's PREFERENCE alone — stamp condition (1) — not on the recon's arithmetic; the R-198 + R-201 equivalence is retained WITH its conditioning class: on the BANKED passive kernel, whose relaxational part is the gradient of the BILINEAR COST, drift = 0 ⟺ ∇E₂ = 0 and a root of γ_eff is the only stationary non-vacuum route THERE; it does not reach a member whose bond COST carries a permitted quartic, and P1.9's object is the exhibit (|∇E₂| = 91.92 there, not 0; 60 SD steps at κ = 0 take E₂ 9169 → 6893 — R-201 intact, its scope was not).] The class is NOT closed **and cannot be closed by enumeration through the 4b instrument**: JD-7 (landed 2026-09-18, `jd7_quartic_2026-09-17/JD7_NOTE_2026-09-17.md`) counted the permitted bidegree-(2,2) plaquette functions per shared-site pair orbit at the driven group [48] — 17 orbits, 239–1366 per orbit, **15,681 beyond the products of the ten bilinear couplings** in the WIDEST class (all bidegree-(2,2) pair functions, [48]-invariance only), and still 99–281 per orbit at the full point group [1152]; **BY CLASS (the result review, 2026-09-18): 1,505 at leading quartic gradient order, and exactly 4 in the (c) member's own full-O(4) class — the (c) member is one of four, and the three siblings are ENUMERABLE (≈ 6 h on the 4b instrument; paths ledger P1.8).** **P1.8 RAN (2026-09-18, `g8_n4_soliton_scoping_2026-09-02/FORCED_ACTIVITY_O4_SIBLINGS_NOTE_2026-09-18.md` REV 1): C fell to the exact vacuum; S_b printed FALLS by the frozen bar on an endpoint that is a converged NON-VACUUM four-site π-cluster the instrument is blind to (a CANDIDATE object, P1.10); S_a / S_c — one functional on every object visited (S_a = 4·S_c = S_b + C on slice objects) — UNDECIDED at the registered budget with degree −1 still carried; zero retained [SUPERSEDED 2026-09-18/19 by P1.9: S_a / S_c were DECIDED by the continuation — a non-vacuum critical point of the quartic-inclusive COST (23186.20; re-based |g|₂ 1.19e-3; 703 plane sites; degree −1) held by an exact E₂ ↔ κE₄ cancellation, its localization undecided until the box read; the tally is now C fell to the exact vacuum, S_b printed FALLS on a four-site π-cluster the instrument is blind to, S_a holds a non-vacuum critical point with a winding number — and S_c is NOT decided by it (S_a = 4·S_c only on the slice; the reviewer measured |g|₂ 0.43 under S_c at the same state); 'ZERO RETAINED' IS WITHDRAWN], with the affirmative word deleted at the freeze (no ray could have been WORDED retained). The recon's rule is NOT satisfied for the class; the ground of this node stays 'the members computed' with that tally. **THE TRIGGER'S WORDING RULED (the coordinator, 2026-09-18; RUL-135 amendment 3):** 'a critical point of the COST at which the maximal plane rotation angle max(|φ₁|, |φ₂|) > π/2 at a site AND a residual COST excess above the vacuum' replaces 'a core-retaining critical point' — the site-angle core counter cannot see single-plane π defects, the plane-angle read can. On the deposited S_b endpoint's printed numbers (four sites at ≈ 177°; residual COST 663.5) the trigger's LETTER is met; whether it FIRES turns on P1.10's registered read (a critical point certified in the full space and HELD by the quartic — not a lattice-pinned defect of E₂ alone) and on the coordinator's stamp; the node is unchanged in this pass.** The widest permitted set is a vector space of that dimension, not a list of members, so 'compute each member' is not a finite task there. Would change if the S_a / S_c continuation (P1.9) converges to a critical point of the COST the re-worded trigger counts (a plane-angle count ≥ 1 with a residual COST excess), or the S_b π-cluster's own read (P1.10) shows a localized non-vacuum critical point the re-worded trigger counts, or a further theorem cuts the 1,505 (locality / the continuum limit), or a narrower menu pick is recorded (RUL-048). REVERT: strike the node ⇒ local activity returns to V4-ACTIVE's CANDIDATE premise, H9 unchanged, S5's text unchanged, the recon reverts to 'the members computed have fallen'. The node FIRES (re-opens) if a permitted quartic term is EXHIBITED and holds a critical point of the COST at which the maximal plane rotation angle max(|φ₁|, |φ₂|) > π/2 at a site AND a residual COST excess above the vacuum on the 4b instrument (the wording of 2026-09-18; the earlier 'core-retaining' wording could not see the S_b cluster) AND a VACUUM SHELL — the bond strain below a registered bar outside a registered radius, the cell named (amendment 4, the coordinator 2026-09-19; the numbers set by the P1.11 box read — until it lands the trigger cannot fire on the P1.9 object) [VACUOUS AS WORDED — the P1.10 keeper 2026-09-19, computed: a uniform rotation zeroes every conjugation-invariant bond read (0.0000 π outside every radius) while meeting amendment 3 at 32 768 sites, so the bond-strain shell excludes nothing it was written to exclude; the replacement quantity is the coordinator's word (class 3) — the keeper's candidate: the per-site COST excess density, ≈ 3000× apart on the two worlds; P1.11's shell scales are registered as DATA, not as this clause's bar, and no trigger fires on amendment 4 until it is re-worded] [P1.11 LANDED 2026-09-19 and CEREMONIED 2026-09-20: the P1.9 object is NOT BOX-CONVERGED at 32³ (CORE-CHANGED on 24³ and 48×32×32; the core COST ±11–24 %; the winding −1 unchanged); the keeper's computed finding: amendments 3 and 4 share ONE root (a fixed-frame quantity against bond-invariant witnesses) and P1.11's own K-4 texture on 48×32×32 meets amendment 3's letter on BOTH conjuncts — the keeper recommends BOX-CONVERGENCE under an independent cube doubling as the single replacement clause; escalated to the coordinator; the GROUND (PARTIAL) is strengthened, not changed, on either branch; P1.11b (64³) is the cube point] [THE CLOSURE RECORD: `knowledge/audit/g8_n4_soliton_scoping_2026-09-02/SOLITON_BASELINE_CLOSURE_2026-09-20.md`; the S5-LOCAL trigger has NO static referent left — its next referent lives on the driven face (P4.1 → P4.4) and the clause as worded (COST-face) cannot certify a driven object] [P1.11b LANDED 2026-09-20 13:11 — the LAST static read: UNDECIDED at the budget on both witness lists; the converged path control, unread by the frozen rule, outside every banded witness's band but the degree's (the core COST −21.8 %, the core Derrick ratio +11.0 %; the degree −1 unchanged; the far field decays on the cube); amendment 5's clause PERMANENTLY UNSATISFIABLE for the P1.9 object; the static sector CLOSED (RUL-137); the object stored as a grain-scale baseline, the trigger's next referent lives on the driven face (P4.1 → P4.4)] [ADOPTED 2026-09-20 (RUL-135 AMENDMENT 5, the coordinator): the trigger's object is — "A localized non-vacuum COST-critical point with max(|φ₁|, |φ₂|) > π/2, residual COST E > 0.05 E*, and certified CORE-BOX-INDEPENDENCE under an independent cube doubling (32³ → 64³) on its registered core witnesses and Derrick ratio." — amendments 3–4 SUPERSEDED; no object has met it; P1.11b is the certifying instrument and the LAST static box read (RUL-137: the static Ω₀ = 0 sector closes on its landing)] — JD-7 ran 2026-09-18 and could not fire this trigger by construction (it returned a dimension and an identity, never a member run); its result review then EXHIBITED the class: the three remaining O(4)-invariant leading-quartic siblings of the (c) member (paths ledger P1.8) were the fireable terms and have been run (P1.8): C fell, S_b printed FALLS on a non-vacuum π-cluster, S_a / S_c UNDECIDED — the fireable term is now whatever P1.9 / P1.10 return — **P1.9 RETURNED ONE (2026-09-18 17:14, UNCEREMONIED: S_a's continuation settled on a localized non-vacuum critical point, COST 23186 with 60 % in the quartic, re-based |g|₂ 1.2e-3, 703 sites above π/2, degree −1 kept — the trigger's LETTER is met; the FIRING waits on the three-checker ceremony, the holder read (LEAVES at κ = 0?) and the coordinator's stamp; `g8_n4_soliton_scoping_2026-09-02/FORCED_ACTIVITY_P19_CONTINUATION_NOTE_2026-09-18.md`)** — or if a co-moving relative-equilibrium search finds a rotating attractor (a ROTOR-RATE-face read: the attractor's rate ω, not a COST; the m = E₀ bridge does not reach it). Record: `g8_n4_soliton_scoping_2026-09-02/FORCED_ACTIVITY_C_NOTE_2026-09-17.md` REV 5–11; the (d) note; the worklist ★★ bullet. |

### Core node — the weak-isospin host (added 2026-08-21, RUL-082; the first C-32 exhausted-menu promotion)

| Node | Status | Menu | Resolution / branches | Revert clause |
|---|---|---|---|---|
| **WK-SD** — which subalgebra hosts weak isospin | **PROMOTED OUT OF ENDORSED (RUL-082, 2026-08-21): `forced-given-{A-P2 + the right-handed-singlet datum}`.** No longer a pick — the menu it was picked from is CLOSED by computation, so under C-32 there is nothing left to pick. **★ THE CONDITIONING PREMISE NOW CARRIES A STAMP (RUL-084, 2026-08-21, human ruling "(b)"): A-P2** (weak isospin is a 3-dim `su(2)` inside grade-2 `so(4)` *at all*) is **FRAMING in the engine and stamped ENDORSED** — a PREFERRED DIRECTION of the family, occupying S7-1's vacated slot in the eight-item endorsed list. **Consequence: the COLUMN OF ONE CLOSES** — `weak = SD` is forced given an *endorsed* premise plus a datum, so its consumers (R-058…R-062, R-077, R-079, R-082) sit in the ordinary **CORE+ENDORSED conditional column**, and divergence at this node happens one level down, at A-P2, exactly as at any other preferred direction. It is still neither an axiom nor an endorsement itself. **★★ SHARPENED 2026-08-23 (RUL-091, human ruling "1. RIGHT") — THE SIDE AND THE MODULE ARE NAMED.** The node's premise is now **A-P2′**: weak isospin is hosted by a 3-dim `su(2)` acting by **RIGHT (body-frame) multiplication on the even-subalgebra module** of the substrate's rotation algebra. Previously both the side and the module were **silent** (the corpus never chose; `weak_su2_menu_exhaustion`'s docstring slid to the spinor module in six unargued words — keeper C2). **The left / space-frame reading is RETIRED as internal host:** the left-centralizer theorem gives `Z_left(so(1,3)) = span{1, e₁₂₃} ≅ ℂ` — a U(1), never an `su(2)` — and weak isospin and flavour are Lorentz-scalar labels, which a left host is not. **What survives verbatim (`ADJUDICATION_R3_2026-08-23.md` §4-bis, coordinator-verified):** SD ⊂ `H₊` and ASD ⊂ `H₋`, the two CENTRAL ideals of the even algebra, so the annihilation structure is an **ideal fact and side-independent** — body-SD annihilates `W⁻` and acts full-rank on `W⁺`, body-ASD mirrored — and `[L_A, R_X] = 0` **exactly (0.0)** over the observer generators `{J_i, the banked grade-one boosts}` × `{SD, ASD}` on the full 16-blade basis. Hence **V−A is NOT forfeited** — *that inline clause is SUPERSEDED as worded; it holds on the `I₄` eigen-halves and reaches observed chirality only given the bridge in the stamp that follows* [**STAMPED 2026-09-22 21:48 — the external review's lemma, reproduced by the developer on the engine: in the corpus's own LEFT-ACTING Dirac realization (dossier §B.3.7, R-026) this cannot hold — if Γ² = 1, {Γ, γ̂} = 0 and [T, γ̂] = 0 then T(1 + Γ) = 0 forces T = 0, and a right-acting host commutes with every left multiplication (feasibility residual 7.41). V−A is forfeited THERE and survives only GIVEN THE BRIDGE; the two-sided realization γ̂^μψ = γ^μψe₄ is the CANDIDATE route (residual 1.6e-14, the host faithful on the half it keeps), and it lives only on the FULL Cl(4,0) — not on Cl⁺(4,0), V4-0's signed carrier, not on Cl⁺(1,3), not on the Dirac ideal S — so the route COSTS THE CARRIER PICK, the coordinator's call: `knowledge/audit/external_review_v3_2026-09-22/`**], and the menu closure below carries to the body copy as **algebra** unchanged (RUL-082's revert clause fires in the annotate-not-revert direction). Open on this branch: the module choice itself is owed as a banked primitive (the r3 reviewer's draft enters as CANDIDATE), the `I₄`-grading ↔ observer-chirality bridge (applies to both branches), and the ℂ-linearity constraint | **The COMPLETE list of 3-dim Lie subalgebras of grade-2 `so(4)` up to conjugacy — computed, not asserted** (`weak_su2_menu_exhaustion`, MAIN engine; finite Goursat sweep over `(dim p₁, dim k₁, dim p₂, dim k₂)` whose Lie-theoretic inputs are the engine-exact structure constants `T = c·ε`): **{SD · ASD · the diagonal `so(3)` class = {Stab(v)}, of which the L-orbit `Stab(e₄)` is one member}** — exactly three, and the earlier corpus menu of *two* was incomplete. **They are not three of a kind:** SD and ASD are IDEALS, hence conjugacy classes of size one — rigid points the connected group cannot move — while the diagonal class is a genuine 3-parameter family (an `SO(3)`-worth of graphs). *That asymmetry is why only an OUTER, orientation-reversing automorphism can reach across.* **Where the exclusion comes from:** lemma L1, *`su(2)` has no 2-dimensional subalgebra* (because `rank su(2) = 1`) — the handed-mixture residuals below are a SUB-CASE cross-check and must never be cited as the general argument. Cross-checked: the Q-orbit and the whole handed-`SD`/`ASD` mixture family fail to close as Lie algebras (relative residuals 1.00 / 0.39–1.00) and are not on the list, as the classification requires | **ASD — REFUTED AS A RIVAL, not as a physics claim: it is the same member mirrored.** An orientation-reversing frame reflection (induced vector map det = −1) maps SD onto ASD at residual 2.2e-16 — verified on **three inequivalent reflection vectors** including a non-basis one, so the exchange is generic rather than a basis artefact — preserves the diagonal **class** (membership; the `e₄`-axis-preserving reflectors fix the L-orbit itself, a generic one carries it to `Stab(v')`, residual 0.707 — asserted nonzero so the class-vs-member distinction cannot be lost again), and flips the `I₄` sign that *names* the two factors ⇒ up to `Aut(so(4))` the menu has TWO classes, not three; "picking ASD" is a global relabelling of spatial orientation (RUL-051 family freedom; **would-change-if:** any independent physical object that pins the substrate's orientation — none is banked). **DIAGONAL `so(3)` CLASS — REFUTED BY DATA.** Not by the neutrino: **restricted to the neutrino's own Weyl half the L-orbit and SD span the SAME algebra** (image dimensions 3/3, union 3 — engine-computed; the corpus's long-standing sentence *"a single-Weyl neutrino excludes weak = L-orbit"* is **FALSE as stated**, and is repaired at every use-site in the LIVE corpus in this pass — engine, harness, paper, companion, ledgers. **Two classes of site are deliberately NOT repaired here:** `CLAUDE.md`, which only the human coordinator edits (the exact diffs are written out at `knowledge/audit/consolidation_2026-08-21/CANON_DIFFS_WEAK_SD_2026-08-21.md`), and dated/archived records, which are history and correctly keep the state they were written in). The discriminator is the OTHER half: `image_dim(SD\|W₋) = 0` (weak-singlet sector) vs `image_dim(L\|W₋) = 3` (charged at full strength). **"Dimension" here is the IMAGE dimension of `h → End(S)` — faithfulness of the subalgebra — never the matrix rank of a single operator** (a single nonzero SD bivector acts invertibly on its half); the engine returns both so the ambiguity cannot be re-introduced — so a diagonal host makes the right-handed fermions a second weak doublet sector, which the observed weak-isospin-**singlet** character of the right-handed fermions forbids. Consumes CORE LS + `forced_handedness` (R-075) + the two-ideal Dirac occupancy (R-076) + **the ENDORSED premise A-P2** (RUL-084) + that datum — **one endorsement, no V3 pin** | **Reverts to ENDORSED-annotated** ("empirically closed at family level; no surviving Core-compliant candidate found on the non-SD branch") if **either** A-P2 is re-stamped away from ENDORSED **or** the closure is refuted. A refutation would have to do one of: exhibit a fourth conjugacy class (the sweep is the thing to attack); pin the substrate orientation, which promotes ASD to a real branch; or exhibit a family member whose matter occupies a single Weyl ideal, which empties `W₋` and voids the datum's bite — that member has no charged-lepton Dirac mass. **What falls with it:** V−A, generation-blindness, the doublet structure and up = SD return to DERIVED-given-an-INPUT-bit; §C.4.2's menu-and-pick reading is restored. **★★ ADDED 2026-08-23 (RUL-091): the side/module revert is SEPARATE and reads — re-rule LEFT.** If the coordinator re-rules the host to the space-frame (left) action, restore the left-module reading at every site named in the RUL-091 register row (charter third dated note, this node, companion rows R-058…R-062/R-077/R-079/R-171, the `twt.py` A-P2 and `weak_su2_menu_exhaustion` docstrings, §B.3.1's division-of-labour sentence); the §4-bis computations are **side-neutral facts and stay**. A change of the hosting MODULE or SIDE now also fires RUL-082's own repaired clause |

### Core node — S4's realization (added 2026-08-23, RUL-094 Q5; recorded, not picked)

| Node | Status | Menu | Resolution / branches | Revert clause |
|---|---|---|---|---|
| **S4-REAL** — HOW the protected pattern is realized | **OPEN BRANCH (recorded, deliberately unpicked).** S4 says "protected pattern," which covers both readings; V3 realizes one of them and the charter text needs no edit either way | {**real-space soliton** — the winding defect of §C.1, V3's realization, `π₃(S³)` topological charge in the rotor field \| **spectral node (Volovik-class)** — the protection lives in the *dispersion*: Weyl points in the grain-level spectrum, with chirality, near-node emergent Lorentz invariance, gauge fields and Chern-number charge coming free of the node structure. OPEN BRANCH} | The spectral branch's cost is **fermionic grains** — a local state carrying fermionic statistics rather than a bosonic orientation — which is where it meets **LS-ℤ₂**: the two open questions merge into one, and the discriminator is the same object, the **LS-ℤ₂ bond energetics** (whether any banked or admissible bond term is odd under the local sign flip, and in which bond class). Nothing on the spectral branch is built; the probe arc that would map what it buys and costs is a PRIORITY docket item (`TWT_worklist.md`, current docket item 8) and is CANDIDATE-tier throughout | **The node stays as record.** No pick is made here, so nothing reverts; if the spectral branch is ever taken, it is a new instance leaf, not an edit to this node, and the LS-ℤ₂ node's revert list travels with it |

### Node — WHAT THE BODY-ASD TRIPLE CARRIES (added 2026-08-23, the body-frame adjudication; **OPEN — recorded, no pick taken**)

> **Scope banner, read first.** This node is recorded because RUL-091 named the weak host's
> side and module and its **mirror half was never given the same treatment** — one half of an
> SD/ASD pair now carries C-33 discipline and the other does not, and that asymmetry is what
> made the external submission's "generations are homeless" proposal look plausible. **The V4
> branch is NOT opened here and no branch table is created**: the carrier node (which module
> the internal host acts on) and the opening of V4 as a family member are **escalated to the
> human coordinator** under RUL-048, unenacted. What is enacted is this node, OPEN — the
> question, its menu, its computed Layer-A facts, its named latent tension, and the two
> computations that would decide it. Nothing below is a pick and nothing below rides anything.

| Node | Status | Menu | Resolution / branches | Revert clause |
|---|---|---|---|---|
| **V4-ASD** — what does the **body-ASD** triple carry? | **OPEN — this is the defining question of the proposed V4 member, not a made pick.** It supersedes and merges the submission's two separate proposals (V4-LR "right-handed fermions are body-ASD doublets" and V4-G "generations ≠ ASD"): they are **one** question, because both ask what the body-ASD copy is for, and opening either alone opens half of it — V4-LR alone omits the cost, V4-G alone was **rejected at the root** (its demotion of the seat count is unearned: `dim Λ²₋(ℝ⁴) = 3` is a **dimension count on ℝ⁴** that names no module and no side, so a ruling about which side a *different* algebra acts on cannot orphan it; and its second leg is answered on its own terms — the body move commutes body-ASD with the observer exactly as it commutes body-SD, `[L_g, R_h] = 0` for **all** `g, h`, so the move **rescues** generations = ASD rather than orphaning it) | (i) **GENERATION SEATS** — V3's inherited reading, used as an **index set**; the generation OPERATOR is the ℤ₃ meta-time phase (`generation_z3_is_metatime_phase`: *"Count-3 stays geometric (ℍ-triple); only the placement is dynamical"*) · (ii) **A GAUGED `SU(2)_R`**, broken at a scale — the left–right member; carries a `W_R` · (iii) **BOTH**, which requires a written passage showing an adjoint index set survives gauging; **no such passage exists** · (iv) **NEITHER** — body-ASD is not physically realized and both jobs relocate | **LAYER A — the local field module (`Cl⁺`), computed:** `image_dim(R_ASD\|W₊) = 0` and `(R_ASD\|W₋) = 3` **exactly**, on the operator-span reading of the **I₄ grading** — body-ASD grips exactly one half of that grading and is **identically blind on the other** (`weak_host_must_be_body_frame`, banked 2026-08-23). That single number is simultaneously reading (ii)'s **asset** and reading (i)'s **cost**. Both readings are Lorentz-scalar and associativity does **not** discriminate between them. **LAYER B — the collective spectrum:** both readings are obstructed under step **S4** (the defect's collective manifold = the full `Spin(4)`, UNBUILT and #1-gap-routed) — a body-ASD **triplet** drags lab spin 3/2 inside one irreducible block (`PROBE_PW` §1.3), and with body-parity unbroken the two body-chirality blocks are **exactly degenerate**, doubling the lowest baryon multiplet, which is not observed; and under the corpus's only **BUILT** band — the one-inertia ANW band (`skyrmion_rotational_band_nucleon_delta`, `M_N` −0.3 %, `M_Δ` −0.18 %, split +0.10 %) — there is **no body label at all** and both readings fall. **LATENT TENSION (fires the moment reading (ii) is stated as *gauged* rather than *permitted*):** the three ASD planes **are** the Lie algebra of `SU(2)_ASD`, so gauging it makes whatever they index an **adjoint (j = 1) multiplet**, degenerate up to breaking. **Graded as an FCNC-GATED DEBT, not a refutation** — gauged horizontal/family symmetry broken at a high scale is a **published model class** (prior art, meta-observer F3: this collision is not novel physics), and hierarchies are routinely generated by the *breaking sector* rather than by the unbroken gauge structure, so adjoint degeneracy *before* breaking does not by itself contradict `m_e : m_μ : m_τ ≈ 1 : 207 : 3477`. The sharp empirical instrument is **flavour-changing bounds** on a gauge boson carrying generation number, which at a TeV-scale breaking are severe. The debt is therefore: *the breaking sector must generate the entire hierarchy AND clear FCNC bounds at the breaking scale.* **THE ESCAPE IS NAMED, and it is why this is a tension and not a wall:** the tension fires under the **SEAT reading** (§C.3.8, §D.2.4, Core §2.3, canon §5) and **NOT** under `generation_z3_is_metatime_phase`'s **index-set** reading, where the acting algebra is the meta-time phase and the ASD triple is only a label set — the corpus carries **both**, and menu item (iii) is exactly the demand that this escape be written out rather than assumed. **THE `W_R` EXPOSURE LIVES HERE, IN THIS CELL, AND NOWHERE ELSE.** It is **prediction-shaped, mechanism-gated, and unpriced**: (a) it is an **exposure, not a falsifier** — "a null result at every scale" is not an achievable experimental outcome, so a row phrased that way could never fire; (b) it is a **MECHANISM** question before it is a scale question — `PROBE_PW` §3.4 computed that the SD↔ASD exchange **is** the substrate's own spatial parity (conjugation by `e₄`, which fixes the drive axis *and its direction*), so **nothing banked breaks `SU(2)_R`**, and a labelling asymmetry (`weak = SD`, `generations = ASD`) is not a dynamical breaking; (c) the corpus banks a parity-odd background (`⟨I₄⟩ ≠ 0`) which anchors *which factor is which* but, being a gauge singlet, supplies **no coupling asymmetry**, so **no scale follows**; (d) axiom-arc keeper K3's fork — whether the SD/ASD distinction is physical at all (§C.5.3's parity-odd condensate vs `weak_su2_menu_exhaustion`(2)'s relabelling-only conditioning class) — is **UNCLOSED and must close before any `W_R` sentence**, because a `W_R` consumes exactly that distinction. **No `W_R` bound is quoted:** the corpus carries none read from a primary source by our own tools, and the externally-offered "≳ 5–6 TeV" is **struck** pending one (the standing rule after the fabricated-LISA-PF and Astafiev incidents). **JURISDICTION, ruled:** this exposure does **not** go in the Core paper's §4.2 — that table is **family-level** by its own words (*"a positive detection on any of them reaches every candidate at once"*) and a `W_R` reaches **one member**; nor in §5.3 ("The two constraints in force" — the section the dossier and the ledgers call the wounds), which says *"Both bind this candidate"*, i.e. V3. **Consequently the RUL-081 falsifier-count pin is UNMOVED at all FOUR count-bearing sites** — `TWT_core_paper.md:881`, `TWT_foundational_paper.md:1087`, `TWT_foundational_paper.md:7091` (the fourteen/two split) and `TWT_foundational_paper.md:180` (the sixteen-row total) — **and none of them moves at this pass.** **THE TWO DISCRIMINATING COMPUTATIONS, at their two named layers — and they are different computations, neither reaching the other:** **(L-A), the cheaper and the one nameable today:** can a label that **annihilates `W₊`** index the generations of a **charged lepton that occupies BOTH Weyl ideals** (R-076, its Dirac-mass channel)? Engine-reachable now, no kernel. **(L-B), the keeper's standing commission:** compute `H = Stab(R_sol)` for the banked `B = 1` hedgehog inside the global symmetry group of the banked energy functional and return `dim(G/H)` split spatial/internal, asserted against the ANW count — Layer 1, no kernel; it settles **S4** and therefore Layer B **for both readings at once**, and `PROBE_PW` §3.2 already computed that both natural stabilisers kill the left–right spectrum outright (`H = SU(2)_ASD` leaves no second body doublet; `H = SU(2)_diag` empties the fermionic sector of the coset). **Knowability:** PINNABLE in principle (a `W_R` exists or it does not); **#1-gap-routed for the scale** — RUL-030 class (2) for the value, class (1)/(3) for the structure | **Nothing reverts, because nothing is picked.** Whenever a pick IS made here, the other menu item's use-sites must be relocated **in the same pass**: picking (ii) fires §C.3.8, §D.2.4, Core §2.3, §E.3 row 13 (the no-fourth-generation falsifier) and canon §5's "GENERATIONS = the anti-self-dual triple"; picking (i) requires the member to **name the premise that silences body-ASD** — and our own §C.4.6 / `d4_dm_plaquette_holonomy_explicit` licenses gaugeability **symmetrically** across the two chiral factors (`PROBE_PW` §3.5.1), an **UNDER-CLAIM recorded against us** and still otherwise unrecorded, so "it just isn't gauged" is not free. **Recorded alongside, second UNDER-CLAIM against us:** R-171's closure classifies **3-dimensional** hosts and is **silent** about a 6-dimensional two-scale one, so the menu closure does not by itself exclude reading (ii) |

**STATUS REFINEMENT — OPEN-WITH-FLOOR (2026-08-24, round-4 commission item 4; adjudicated in
MERGE_AND_COMMISSION_2026-08-24).** The FCNC-GATED DEBT graded above is now **confirmed with
numbers**, and the two branches separate:
- **Branch (i) — `SU(2)_ASD` unbroken: REFUTED OUTRIGHT, and this half is a theorem, not a
  literature number.** With the three generations in the **adjoint** of a gauged `SU(2)_H`, a
  mass matrix invariant under the full algebra must commute with all three generators; the
  commutant of the adjoint action is **one-dimensional (multiples of the identity)** — proved by
  linear algebra alone (`04_fcnc_bounds.py` Part A, reproduced this pass; equivalently Schur on
  the irreducible adjoint). So the unbroken variant forces **exact three-fold generation
  degeneracy**, refuted by the observed hierarchy at the first digit. There is **no GIM-like
  escape**: the couplings *are* the structure constants (at best one generator is made diagonal,
  the other two are pure ladder operators, 100 % generation-changing). No primary read is owed for
  this half — it needs no bound.
- **Branch (ii) — `SU(2)_ASD` broken: viable only above a high floor, VALUE QUARANTINED.** A tree-
  level `ΔF = 2` / `ΔF = 1` computation with the forced `O(1)` flavour-changing couplings returns
  `M_H/g_H ≳ 10⁴ TeV` for generic CP phases (`≳ 10³ TeV` real), the operative channel `ε_K`,
  cross-checked in-code against the Isidori–Nir–Perez benchmark (arXiv:1002.0900) at factor-2.
  **This CONFIRMS the keeper's "at a TeV-scale breaking are severe" grading with an actual scale.**
  Three structural costs then bind *inside TWT*: (a) a `≥10⁴ TeV` horizontal factor is inert for
  the ≤GeV generation phenomenology the ASD triple organises (gauging the seat buys nothing
  observable); (b) it owes a breaking sector at a scale the corpus does not own — the same
  "Planckian layer and hadronic layer and no third one" that convicts the `sin²θ_W` crossing, and
  the Majorana-seesaw route to an intermediate scale is forbidden by exact `B−L`; (c) it **pins the
  SD/ASD orientation physically** (one factor light+gauged, one heavy+gauged), contradicting the
  unpinned convention status `weak_su2_menu_exhaustion`(2) assigns it.
- ~~**QUARANTINE (the no-unread-bound rule).** The numeric floor is NOT banked …~~ — **QUARANTINE
  DISCHARGED ON INPUT GROUNDS 2026-08-24: the primary read landed, by our own tools**
  (`knowledge/audit/external_review_r4_2026-08-24/primary_reads/READ_FCNC_INP_2026-08-24.md`;
  the read's own closing scope carries — any residual risk now rests on the lemma/adjudication
  side, not the inputs). Fourteen of the sixteen hard-coded inputs located directly in fetched
  primaries, zero UNREACHABLE, **two [INDIRECT]** (f_Bs√B via PDG's ξ, +1.0 %; Δm_D via
  HFLAV x — neither near factor 2); Isidori–Nir–Perez Table 1 (arXiv:1002.0900 §III.1) read in
  full and the comparison **SUPPORTED** (the read's own verdict word; a ratio comparison at 1–2
  significant figures), all five mixing rows inside factor 2
  (Δm_K 0.96 · ε_K 0.74 · Δm_D 1.51 · Δm_Bd 1.01 · Δm_Bs 0.97); BR(μ→3e) attribution exact
  (SINDRUM/BELLGARDT 88). **The floor `M_H/g_H ≳ 10⁴ TeV` generic (≳ 10³ real) now stands at
  order-of-magnitude on primary-verified inputs**, with three read-findings carried: (F-1) the Δm_D row is
  quoted at the primary's own conservative `≳ 1.2×10³ TeV` (the note's 1.8×10³ was 1.5× above it —
  ε_K governs either way); (F-2) a few-% mixed-normalization inconsistency (RGI `B̂_K` vs
  MS̄(3 GeV) `B_D`) noted, sub-decisive; (F-3) the factor-2 agreement is **criterion-robust, not a
  reproduction** — INP bound NP against the SM short-distance amplitude, the script against the
  measured value; the residual spread is partly that criterion difference, not only
  vacuum-insertion tolerance. Largest update-drift found: 3 % on `Λ_εK` from the `B̂_K` choice —
  nowhere near the order-of-magnitude claim.
- **Forward pointer (F-12 of commission ledger 01):** the double-booking rows **R-064 / R-098**
  (and §C.3.1's generations = ASD ↔ §C.4.2's weak host) now point here — the node moved from
  "recorded open" to "open with one branch refuted and one branch floored".
- **Unchanged:** no pick is made; nothing reverts; the two discriminating computations (L-A, L-B)
  stand; and **the RUL-081 falsifier-count pin does not move** (a `W_R` / horizontal boson reaches
  one member, not the family — jurisdiction ruled above).

**Two ORPHANS ride this node and are recorded here rather than lost** (the reconciling passages
do not exist and are owed before any pick): **(1) R-102 ↔ A-P2′** — the corpus names **two
different modules** for the local state's one-sided internal action, the spinor minimal ideal
`S` (R-102 / R-012, §A.5.4) and the even-subalgebra module `Cl⁺` (A-P2′ / RUL-091), and no
passage says which the internal host acts on. `weak_host_must_be_body_frame` **separates them by
an observable** (on `Cl⁺` the full body `so(4)` is available; on `S` the even right-stabiliser is
a single `ℍ` charging both halves, i.e. the diagonal-class occupancy the singlet datum kills) —
which is what makes the question answerable, and which is the **carrier node's real ground**.
**The carrier node itself is NOT opened here: it is escalated to the human with RUL-048
sign-off.** **(2) R-167 rejects `Cl⁺` as a carrier while the carrier node would pick `Cl⁺`** —
R-167's REJECTED ALTERNATIVES clause rejects "full `Cl⁺(4,0)` (`ℂ⁴`, but four blades fail (W))"
as the **Bell-wing** carrier. Reconcilable by role (Bell-wing module vs internal-host module),
and the reconciling sentence must ship **with** the carrier node, not after it.

## PREFERRED DIRECTIONS (ENDORSED — highly plausible; family members may diverge)

grain discreteness (extremely small 4D grains, water-extrapolated) · Skyrmion-class
defects (S4b) · ~~weak = SD (S7-1)~~ **— PROMOTED OUT 2026-08-21 (RUL-082); see Core node
WK-SD above — and this slot is now taken by `A-P2`, the weak-hosting premise (weak isospin
is a 3-dim `su(2)` inside grade-2 `so(4)` at all), STAMPED ENDORSED 2026-08-21 (RUL-084):
eight directions as stamped, nine standing, one substituted, one added** · carrier structure (S5b) ·
measured-G anchor practice
(S7-2) · Koide c = √2 (S7-5) · m = E₀ (S7-7) · generations = ℍ-triple + associativity,
with Horwitz's published adjoint-construction reason (B-7) · the site as an active element (S5-LOCAL, RUL-135;
scope RUL-139(a)).

## BRANCH — INSTANCE V3 (the family's first full leaf; both empirical wounds live here)

| # | Pick (PINNED) | Menu it was picked from | Why (what rides it) | Revert clause |
|---|---|---|---|---|
| V3-1 | Substrate arrangement = REGULAR D4 lattice, at the G_N-back-fit (Planckian) size | regular lattices (D4/others) / irregular-random (causal-set-adjacent) / continuum medium with cell scale | The D4 isotropy results (R-165 class), N_eff = 6, every lattice-dispersion consumer; the dim-6 ceiling arithmetic | Re-arrange or re-size: dim-6/dim-8 numerics recompute; I-3 strike-clause fires; Layer-1 structure untouched |
| — | **V3-1 GATE (narrow form, adopted 2026-08-24, RUL-101/E-4 — APPLIED on the human's word this session).** No candidate may **quote or rely on a dimension-six Lorentz-violation coefficient**, and **no candidate elaboration may proceed on the regular-Planckian-lattice arrangement pick itself**, until either (i) a suppression mechanism is exhibited, or (ii) this pick is retired. **Scope, stated so the gate cannot spread:** it fences *this pick* and *that quotation*. It does **not** freeze the kernel programme, the generations arc, the mass sector, the hadron machinery, or any non-arrangement arc — a gate that blocked the work producing its own discharge condition would be a stop, not a gate. **Revert:** strike this block and the dimension-six exposure returns to being governed by tiering and gating alone (the coefficient already raises), which is the pre-2026-08-24 state. *(The optional canon Site-B line and the drafted Diff-2 reference-class reminder stay UNAPPLIED per the draft's own recommendation — `CANON_CHARTER_DIFFS_DRAFT_2026-08-24.md`.)* | | | |
| — | **V3-1 chain record (P2 residue, Opus inspection, added 2026-08-18):** the wound-bearing chain is refusal→materiality (CORE) → grains→discreteness (ENDORSED) → **regularity-at-back-fit-scale (THIS PICK)** — the coordinator's own stamping annotation *"Lattice consequently imposed from those choices. Every number relative to the size of the monad and lattice: Imposed"* is the chain's record. The dim-6 wound rides the V3-1 pick, not the refusal and not the endorsement. Wound-avoiding family members at this node: the irregular-DISCRETE branch (causal-set-adjacent; BHS-constrained — pays with non-locality or the frame; **the constraint is registered as import I-26**, companion Section 13.5: no finite-valency graph can be associated to a sprinkling consistently with Lorentz invariance, so a member here must state which of {discreteness, Lorentz invariance, finite valency} it gives up) and the CONTINUUM-with-cell-scale branch (priority note per P3: the most valuable unexplored family region; costs the D4 results, N_eff, and the endorsed grain ontology) | | | |
| V3-1a | **Drive-axis alignment = the advance axis is a lattice symmetry axis (`+e₄` of the D4 frame)** — a sub-node under V3-1, the implicit commitment NAMED 2026-08-25 (R-185's §8a meta-observer finding F1: every banked driven-group object is built at `Stab(+e₄)`, and nothing anywhere had recorded the alignment as a choice) and **GRANTED as an explicit pick by the human (RUL-107(2))** | aligned (banked — `\|Stab\| = 48`, the whole driven-group apparatus) / misaligned-generic (trivial stabilizer; spatial degree-4 permission dimension **15**; the driven-group machinery collapses) / aligned-to-a-lower-symmetry lattice direction (intermediate stabilizers) | Every `Stab(+e₄)` object: the Γ-referent block and its ten-constant menu, R-185's invariant theory, the V3-2a support pick, R-186's dispersion split, the DM chirality-lock counts | A misaligned member recomputes every driven-group count from its own stabilizer; R-185's would-change-if is keyed axis-independently (W(F4)-orbit constancy) and survives any alignment choice. **The RUL-107(5) flagged adjacency RESOLVED CONFIRMED (RUL-110, 2026-08-27):** the relation the human flagged is UNLOCK-5's computed content — this pin is what makes a second defect rate referent-bearing at all — and **V3-1b (M-3a, granted) now rides this node: if V3-1a reverts, V3-1b reverts with it** |
| V3-1b | **M-3a GRANTED — the two-rate (non-simple) defect rotor, DRIVE-REFERENCED** — sub-node under V3-1 placed beside V3-1a on the human's own adjacency directive (RUL-110, 2026-08-27: *"adopt M-3 coordinated with the drive-axis alignment. They have to be close from each other given their inter-dependency"*; enacted as **M-3a only** — M-3b NOT granted, M-3c moot). The defect's twist generator is a general Spin(4) element with two plane angles — transverse (L) and wave-parallel (Q) rates, separately invariant **only given V3-1a** (UNLOCK-5 computed: weight drift 5.6e-16 under `Stab(+e4)`, 0.60 without the axis). Removes SS A.4's unargued "simple" restriction (one-rate classes are measure zero, 0/20000; the banked mass blade already splits SD:ASD exactly 0.5:0.5). **Adds ONE counted real: the rate ratio — kernel-routed, unpinned.** `mass = omega` survives exactly (the observer reads the winding-plane angle, 3.3e-09, every second axis); the mass payoff of the second rate is a #1-gap IOU (`omega = F(theta)`, C2 swept: no banked candidate). **Live empirical exposure until C1 computes:** matter-wave amplitude beating `|cos(Omega t/2)|` (exact to ten digits); the lock-kills-the-beat escape is asserted-not-computed. **UNLOCK-4's axis recommendation (pure-ASD off `(1,1,1)` — Z3-breaking yet weak-blind) is recorded CANDIDATE, NOT picked** — which axis is a further node. | simple/isoclinic-locked (the old restriction; stays the leading ansatz of every banked computation) / **two-rate drive-referenced (ADOPTED)** / free-standing two-rate without V3-1a (NOT AVAILABLE — no invariant referent, a coordinate artifact) | SS A.4's u-hat sentence + half-angle paragraph + two-faces gloss (amended per the blast radius's honest-repair wording); R-005 companion gloss; **R-178's would-change-if FIRED** (premise false in the adopted physical case; conclusion retired-where-used; algebra stands conditional) with JD-6 / JD-6(b) / N62 / R-176 swept; **JD-6 OPENS to computation** (m = 3 at |c3| = |c1| = 6.00 on banked J); **N64 CONVERTS** to the F-map debt (direct-readings kill stands); the E(a,b) + C1 + C2 computations are this node's owed entry work | Strike this node; restore "simple" + the two-faces gloss; un-fire R-178; revert N64's conversion; close the beating exposure. **REVERTS AUTOMATICALLY IF V3-1a REVERTS** (no referent without the pinned axis — the inter-dependency clause is executable, both directions recorded here and at V3-1a) |
| V3-10a | **Cell-scale target space = the SAME target as the grain state** — named 2026-08-26 on the round-5 external report's §5.5 ("the largest assumption in the candidate and it is not among the recorded branch points"); the corpus builds the Skyrmion on an `S³`-class target while the grain state is the 4D orientation, and no row recorded the identification | same target as the grain state (banked usage) / the **Goldstone coset of the grain vacuum** (what a symmetry argument would deliver) / an unrelated emergent target (a genuinely new cell-scale field) | The whole hadron sector — `π₃ = ℤ` is what makes a Skyrmion exist at all, so R-144's calibration chain, `f_π² = 8J/a`, the ANW toolbox (V3-10) and every hadron number ride this identification | A surviving target of dimension < 3 carries `π₃ = 0` ⇒ **no Skyrmion, no hadron sector** and the calibration table loses its carrier; a three-dimensional surviving target promotes the identification from assumption to **derived-from-below**. The deciding computation (the surviving manifold's dimension + a gap comparison; LSWT machinery already banked) is the docketed Goldstone-count probe. **Recorded as OPEN — the banked usage is the first branch, not a ruling** |
| V3-2 | Bond structure = the {J, D} truncation | the ten-constant D4/driven bilinear menu (J:2 + D:2 + Γ:6; §8a-verified 2026-08-18) + pseudoscalar χ (vanishes at the driven group) | The canted-vacuum numerical spine (see the ★★ J,D/Γ REWORK block) | Turn on the surviving Γ direction / the second D dial (the DM-support half of this pick now has its own sub-node, **V3-2a**): the rework's re-ordered scope executes; quadratic spine conditionally protected — the Γ survivor is quadratically inert and exactly zero on both computed vacuum branches, while the second D is a **leading-order** actor. Measured risk ordering, carried forward: **`D_spatial` > Γ**, within the frame-bilinear class. **★ ANNOTATED 2026-08-23 (Γ-channel referent closure, `knowledge/audit/gamma_referent_2026-08-23/`; engine `gamma_survivor_pitch_genericity` + `bond_channel_parity_exclusivity`) — the ordering's two halves do NOT carry the same conditioning, and the repair is ASYMMETRIC rather than a blanket hedge:** the **`D_spatial` half is now BRANCH-INDEPENDENT** — by the exact channel→parity selection rule (`Tr(K Wᵀ) = Tr(Kᵀ W)`, a trace-pairing identity holding for arbitrary real `W`, no helix and not even a rotor required), `D_spatial` is the ONLY channel in the exhaustive ten-member menu that can reach the parity-odd amplitude, at ANY configuration; the **`Γ` half is BRANCH-CONDITIONAL** — the survivor is exactly inert only on the two banked high-symmetry `k̂`, and off them its pitch entry is nonzero at a few percent of `J`'s own even weight worst case (~0.1% median, equal per-bond scale; the round's own headline `7.1e-01` was a bare number compared against nothing and is **WITHDRAWN**). So the sentence as written conditions on the CLASS and not on the BRANCH, and its `Γ` half inherits the branch risk in full (GR-1; keeper latent **L-2** is UPHELD-not-discharged and GR-1 FEEDS it; node **V3-2a** is ANNOTATED-not-discharged) |
| V3-2a | **DM support = the 12 `e₄`-bonds only (`D_spatial = 0`)** — a sub-node under V3-2, recorded because the rework showed no substrate argument makes it | the **2-dim allowed DM space at `Stab(+e₄)`** — {`D_e₄`, `D_spatial`}, disjoint bond support, each forced exactly 50/50 SD:ASD; **no substrate argument separates them** (four routes tried in canon §4 form: parity/point-group — both are basis vectors of ONE allowed space and every larger group kills the DM channel entirely; spatial-chirality — the corpus's own `e₄`-bond D *already* drives a spatial helix, the DM energy being identically zero on an `e₄`-axis helix; chiral content — both forced 50/50; energetic inertness — the second D is a leading-order actor, not a quartic correction) | The canted-vacuum spine (§D.4.3) and the parity-odd amplitude `B` of the 0.79 calibration | **`D₂ ≠ 0` established ⇒** (i) `B` re-reads as `D_e₄ + β·D_spatial`, so the amplitude identification behind `D/J ≈ 0.79` moves; (ii) **the canting can switch OFF**: both dials couple to the *unique* [48] reflection-even chiral linear invariant `Σ_{a≤3} k_a B_{a4}` with fixed coefficient ratio **−2**, so a cancellation line **exists normalisation-independently** (its *location*, `D₂/D ≈ 0.5` at linear order, is normalisation-dependent) and on it the chiral-symmetry-breaking spine goes away; (iii) **coupled to the Γ survivor (latent collision L-2, keep the two findings together):** below the cancellation the vacuum's `k̂` sits on a body diagonal and above it on a coordinate axis, and the survivor's exact vanishing is a property of the vacuum's *high-symmetry* `k̂` — so any `D₂` that moves `k̂` off those directions **exposes the Γ survivor**. **Named CANDIDATE-for-exclusion (not a symmetry argument):** the drive-origin story of §D.3.3 — the DM coupling as the *wave's* chiral contribution, hence supported on the bonds the drive singles out — **#1-gap-routed, hence CANDIDATE by definition.** Non-Core; proceeds on this node (RUL-048(ii)) |
| V3-3 | f_π = the ANW fitted value (S7-3) | any cell-scale anchoring | §C.1 hadron numbers | Re-anchor: hadron chain recalibrates |
| V3-4 | D/J = 0.79, lepton-calibrated (S7-4) | any calibration channel; **★ REFERENT REVIEW CLOSED NEGATIVELY 2026-08-23** — the rider *"may measure a J/D/Γ combination"* is REPLACED by the computed statement: in the **frame-bilinear (relative-frame)** realization the driven-group bond-invariant menu is **computed EXHAUSTIVE at ten** (`J:2 ⊕ D:2 ⊕ Γ:6`; four in the rotor-linear realization — the LS-ℤ₂ class pick), and what `0.79` measures is **`D_total / J_effective`**, a ratio of TOTALS: by an exact parity rule `Γ` reaches only the parity-even amplitude and the second spatial-bond `D` only the parity-odd one, with both admixture sizes #1-gap-routed. On the ℤ₃ leg the assignment still rides **JD-5, which stands UNDISCHARGED**. **Still NOT licensed:** the flat assertion *"0.79 is a combination measurement"* (it asserts `αᵢ ≠ 0`, uncomputable). Engine: `DoverJ_calibration_referent`, `bond_invariant_menu_frame_bilinear`, `bond_channel_parity_exclusivity`; record `knowledge/audit/gamma_referent_2026-08-23/` | R-068/R-074 legs; §C.3.11 cross-check — **and the fence is now *never carry a ratio calibrated on one functional into another*** (JD-6(b): three `J_eff` faces — the `f_π` stiffness, the pitch, the ℤ₃ amplitude — coincide only if the `Γ` admixture vanishes) | Γ ≠ 0 established: 0.79 re-reads as a combination; both legs re-fit |
| V3-2 / V3-4 — OBSERVATION (GS-3 keeper item 9, 2026-09-09; not a branch node, nothing promoted) | The `D = 0` sibling has an exactly isotropic, exactly degenerate massless sextet (`H(k) = 12J k̃² 𝟙₆`, DERIVED-A); every `D > 0` sibling has a ~70 % polarization-dependent light-cone split across its six soft branches (68 % along e₁ at D/J = 0.787; 77 % already at D/J = 0.05); the transition is DISCONTINUOUS; `D = 0` costs the V3-4 lepton calibration and everything downstream of it. **GS-4 (2026-09-09, keeper LATENT 2 + the D/J sweep, `gs4_k0_doublet_v2.py`):** the `D → 0` limit behaves THREE ways on the four rotating modes (the twisted ±k₀ doublets, lab q = 0): their polarization split DOES vanish continuously (≈ |k₀|²/2, 0.01 % at D/J = 0.05 — relief on that axis); their pitch/perpendicular stiffness ratio is 2 at leading order at every D > 0 on the body-diagonal branch (2.08 on the axis branch at the calibration — GS-5) and 1 at D = 0 on every branch (discontinuous — no relief); and the helicity-odd cubic `c` DIVERGES as D → 0 (`c·|k₀| ≈ 4.6–4.8` constant; c = 407 J at D/J = 0.05; zero only at D = 0) — the escape gets WORSE on that axis. Reaching for D → 0 as relief must name which axis. **GS-5 (keeper items 3/13): the three ways are ONE LAW at three orders — order-n inter-sector mixing of the single folded band scales as `t^n·D^{2−n}`, so n = 2 (the ×2; GS-3 keeper C-4's six-branch split, measured D-independent) is D-independent hence discontinuous at D = 0, n = 3 (the odd cubic) diverges as 1/D, and the ≈ |k₀|²/2 polarization split is the subleading continuous correction; the escape pays at whichever order the exposure lives.** **GS-6 (2026-09-09, meta-observer U2 / reviewer (d); an OBSERVATION on the kernel's dial space, not a pick):** the canted kernel's linear face about the uniform state has ONE control parameter, Ω₀/(12J|k₀|²) — the carrier in units of the satellite↔slow sector gap: above ≈ 6 gaps the carrier detunes the hybridization (the satellite anisotropies go), below it the same coupling is tuned toward resonance; and for c > c*(Ω₀) ≈ 2.2√(gΩ₀) (banked R-192) the uniform state is linearly unstable at finite k whatever the gap count — the banked hot dial (1, 12, 14) is on that side (the patterned relative equilibrium is its state). A future carrier PICK is bounded in gap units, and by c*, not by an absolute Ω₀ interval. | observation | — (an observation on the existing nodes; no revert) |
| V3-5 | e_ANW = 5.45 (S7-6) | the Skyrme-stabilizer determinations (massless-pion scheme picked partly on the √18/(D/J) agreement) | §C.1.2 hadron numbers | Legs converge → retires; legs split → the √18 bridge dies (§E.2.1, both ways) |
| V3-6 | Gravity route = Sakharov induced, banked action class U2 (B-1) | Jacobson thermodynamic / Verlinde entropic / Lasenby–Doran–Gull gauge gravity / the Paper-2 thermodynamic reading — all taxed by the analog-gravity caution | R-037/R-163, §B.6; the Λ back-fit and with it the dim-6 magnitude's denomination | Re-route: the Λ anchor moves or dissolves; N51's class-scoped uniqueness stays true of its class. **FACE CONDITIONALITY (coordinator-authorized 2026-09-09):** the pick is the Sakharov FORM (`M_Pl² ~ N·Λ²`); its VALUE (c_lat = 21.83, R-163) is a STATIC-face value at two levels — the operator (a flow-resolvent reading moves it ×0.338, Λ_L × 1.73: the operator-choice bracket) and the occupation (a Sakharov sum on a driven-dissipative NESS runs over the NESS fluctuation spectrum, the #1 gap's Im χ / Θ_rel — GATED). Sakharov (1967) is an equilibrium-vacuum computation; the driven-face coefficient is a Θ_rel consumer. |
| V3-7 | Cost pairing = (iv) `t = α₅∘reverse` (B-2) | RUL-018's four-option menu (the no-invariant-pairing theorem half survives any re-pick) | R-168/R-169; the K-O1 certificate scoping | RUL-018's class-B revert list |
| V3-8 | Carrier costed at (k_c/2)² (B-3) | costed / costless / other densities (rides V3-7) | RUL-018(b) consequences; the two-laws reading's implementation | Rides the pairing re-pick |
| V3-9 | Kernel branch = driven-hysteretic (Grade-B class) (B-4) | the full kernel-class menu (the #1 gap); selection criteria wired: h6 amplitude, determinism (probabilities-as-complexity-tool). **FIRST NON-NEUTRAL MEMORY DATUM (N2D + the branch scan, 2026-08-31): the defect-seeded element's history is written into the collective state on TWO AXES WITH DIFFERENT SCHEDULES — its cycle PERIOD relaxes toward the twin's within about one cycle (79% of the gap; fading-class signature), while its PERPENDICULAR-PLANE ORIENTATION CONTENT is stationary-to-growing over 2.6 periods (d/ρ 0.570 vs the twin's 0.0083; hysteretic-class signature), with the band-polarization overlap's approach to 1 bounded at τ ≳ 700 tu. The earlier '31% smaller slip' is WITHDRAWN — a cycle-phase-and-geometry artifact; the cycle-averaged slip difference is −1.3%. This is coherent with the canon's own fact that a defect has MULTIPLE AXES on which to be defective, and it establishes the standing caution that NO SINGLE relaxation timescale may be read as 'the memory kernel's timescale.' The fork is a CLASS question — AND THE ⋆B₀-KICK NULL (n2d_starkick.py, 2026-08-31, the meta-observer's named decisive test) WITHDRAWS the hysteretic-leg signature: a DEFECT-FREE element kicked to ε = 0.5635 holds it undecayed over 2 cycles with every dynamical observable unchanged (period exactly the twin's 34.507, wind quantum 0.99998 × 2π, Δ identical to 1e-6 — even the deepened |a_k| modulation reproduced), so perpendicular persistence is the GENERIC behavior of the exactly marginal ⋆B₀ coordinate, not memory. What stands — RE-TIERED AT R-194 (the op-B state is a RELATIVE EQUILIBRIUM; NO signature on EITHER side of the fork): (a) D's still-drifting period — transverse relaxation onto the RE family, reproduced by the defect-free nulls, so NOT a fork signature either; the named negative stands instead: no bistability among the perturbation classes tried — **and RE-CUT 2026-09-01 (N3c ceremony, COMPUTED): NO MEASUREMENT BEARS ON THIS NODE, so the earlier ‘leans fading’ is WITHDRAWN as an inference the evidence cannot carry.** Attractor multiplicity is INDEPENDENT of τ_mem in both directions (memoryless quintic bistable; long-memory cubic monostable) and the G-8 flow has τ_mem ≡ 0, so it belongs to neither branch; the perturbation-class negative stands as a statement about attractor multiplicity ONLY. (EOM-map Fork A carries the same clause and records that the fork is ORIGINATOR-SELECTED — no checker may rule it.); (b) FRAMING — both measured ops store history in ZERO MODES (op A in the drive-phase Goldstone η; op B's defect as a displacement of ⋆B₀): memory-in-marginal-coordinates, not attractor multiplicity — the kernel-class fork stays OPEN and this datum does not pick. FENCE unchanged: grain layer, CANDIDATE kernel, one dial, one cell, op B only, #1-gap-routed** | Nothing banked rides it yet (ω-only, k-silent) | Free re-pick; no banked physics falls |
| V3-10 | Hadron machinery = the ANW/Skyrme semiclassical toolbox (B-5, import I-5) | soliton-quantization toolboxes | §C.1 hadron spectrum chains | I-5's own excision row |
| V3-11 | Fermionic quantization = the Finkelstein–Rubinstein scheme (import I-20) — **STAMPED PINNED 2026-08-19 (RUL-063), resolving the presumptive stamp the keeper sweep's finding F4 opened when it caught this as a load-bearing pick with NO stamp.** It is an INPUT (a menu pick, not substrate-derived), PINNED (observed fermionicity over-determines it), costing **one bit**, and branch-blind w.r.t. LS-ℤ₂. **Counted — under the bit-accounting symmetry duty of the same ruling: bit-sized inputs are counted for every framework in the reference class or for none, and TWT's bit-inclusive count is never to be quoted against a rival's continuous-parameter count** | fermionic-quantization schemes for solitons (FR ±1 character of π₁(Q_N); alternatives: induced-term routes, I-1's D'Hoker–Farhi class) | R-025, R-131, R-136, R-141, R-142 | I-20's own excision row |

**V3-11 status note (added 2026-08-18, state-space repair pass; cross-ref Core node LS-ℤ₂).**
The odd-character pick is **CONTINGENT-IN-MATHEMATICS but OVER-DETERMINED-IN-DATA.** Contingent:
a non-simply-connected configuration space admits *both* quantization sectors (even/bosonic and
odd/fermionic) and each is internally coherent — nothing in the topology picks one, which is
exactly why the pick needs a node. Over-determined: the even/bosonic sector has **no exclusion
principle**, hence no shell structure and no stable bulk matter — it is excluded by the world
rather than by the mathematics. This is what makes V3-11 the framework's genuine
empirically-anchored fermionic bit, and it is **branch-blind** with respect to LS-ℤ₂: both
branches there deliver identical π₃ and π₄, so neither can pay for this pick and this pick
cannot pay for that branch. **Named discharge route:** a substrate-induced Wess–Zumino-class
term (the I-1 D'Hoker–Farhi class) would FORCE the odd sector from the dynamics and retire the
bit altogether, converting a pick into a derivation.

**Candidate-list reading (RUL-051):** each branch table below the root is a CANDIDATE — a
member of the family's SURVIVING-CANDIDATES LIST (self-coherent + empirically plausible).
Picks at UNPINNABLE nodes (knowability tag, RUL-051) do not multiply the list: candidates
differing only there are one member, many descriptions.

## Branch protocol (standing)

When a branch point presents itself — a new pick needed, or an existing pick challenged —
the agent ADDS THE NODE HERE (menu + pick + why + revert) in the same pass, gives the
coordinator the plain-language one-paragraph version, and: CORE-touching → waits for
sign-off; otherwise → proceeds on the recorded node. A second instance (V4, a branch
sibling of V3) begins as a new BRANCH table beside V3's, inheriting the root and diverging
at named nodes — that is what "a family of candidates" looks like in the file system.

## FAMILY INPUTS — the third commitment class (instituted 2026-08-23, RUL-096; charter §3 dated note)

Entered data and posited premises made at FAMILY level: neither axioms/endorsements nor
per-candidate picks. Every member inherits them; each is counted; each carries its
would-change-if. **Founding membership below — extendable, never silently: a new family input
gets its row in the same pass that banks it (the import-registry pattern).** ENDORSED directions
and V3 pins have their own homes (charter §3; the branch tables above) and are not duplicated
here; A-P2′ is ENDORSED and appears only by pointer.

| Input | Kind | Where it enters | Would-change-if |
|---|---|---|---|
| **The right-handed-singlet datum** — right-handed fermions carry no weak isospin (no RH charged currents) | entered datum (read, not tuned) | closes R-171's menu (the diagonal-class refutation); the A-P2′ chain's second condition; under a body-frame reading it re-reads as a breaking scale (adjudication in flight) | an observed RH charged current reverses the datum: the diagonal-class refutation and every DERIVED-given-{A-P2′ + datum} tier cell re-opens |
| **The charge anchor** — `Q_p = 1` entered (equivalently the `(1/2,−1/2)` doublet anchor) | entered datum | the 15-value charge assignment's absolute normalization; `charge_sector_provenance` marks the boundary (the discreteness and the `c`-free neutrality identity are derived WITHOUT it) | a second, structure-side determination of the anchor converts it to derived (the over-determination pattern); a different anchor value re-scales the assignment and is excluded by the no-thirds/universal-`c` identity |
| **P4–P7** — the four named structural assignment premises of the charge sector | posited premises | the 15-value assignment rides them (with the anchor); P4 forces `Q_p − Q_n = 1` identically; P7 is named AND computed | each premise's own would-change-if lives in the engine at `charge_sector_provenance` / the P-block docstrings; refuting any one re-opens the value table, never the discreteness |
| **`m = E₀`** — the soliton-mass identification (vacuum-subtracted, v = 0 reading) | posited premise (counted 2026-08-12, ruling R3(a)) | §A.4's mass mechanism; adjacent to — never closing — the R-123 residue | a derivation of the identification from the substrate EOM retires the row; a counterexample (a stable defect whose mass ≠ E₀) breaks the mass sector's floor |

**Note (2026-08-25, su(2) standalone review) — INFORMATIVE ONLY, on the right-handed-singlet
row.** The datum's two faces are usefully distinguished — **D1**, the quantum-number statement
(right-handed fermions are weak-isospin singlets), which is what the banked diagonal-class kill
runs on (the engine's *"at full strength"* clause); and **D2**, the null current bound at
accessible energies, which reaches the kill only through the one-coupling clause. **The row's
would-change-if is unchanged and covers both faces.** Nothing here restates the row more strongly
than the row states it (RUL-097 binds), nothing is restructured, and no tier, value, count or
use-site moves. Verified before writing: the engine's `weak_su2_menu_exhaustion` leads with D1 at
both governing strings — the `empirical_leg` field and the EMPIRICAL LEG docstring — with D2
carried parenthetically and the "at full strength" clause in place, so the banked kill does run on
D1. Ground: `knowledge/audit/standalone_reviews_2026-08-25/ADJUDICATION_SU2_2026-08-25.md` §2.3
and §4.2.

## THE V4 BRANCH — opened 2026-08-23 (RUL-098: "SIGN / OPEN / APPLY") — the second candidate, under construction

**Inheritance policy (RUL-098, the coordinator's directive):** V4 inherits V3's
STATIC/GEOMETRIC picks unchanged unless a V4 node supersedes them; **everything #1-gap-related
starts CLEAN SLATE** — verbatim ground: *"I rather have a clean slate for everything #1-gap
related because I feel that the new generation-mechanism with the tongues will give us a good
lever to find a compatible candidate on the #1-gap."* Clean-slate set: the kernel/memory
branch (V3's hysteretic-vs-fading pick NOT inherited), the drive profile, and every
D.5-adjacent dial; boundary cases (the pairing/carrier-cost pick, the gravity route) are
**inheritance-TBD-at-first-use** — the first V4 result that would consume one must first
record the inherit-or-reopen call as a node here. Calibrations (f_π, D/J) are fits, not
picks: V4 re-fits as its structure demands.

**The #1-gap knowability header (RUL-098, methodology — binds every V4 kernel arc).** The
coordinator's ground, verbatim: *"We didn't model the water molecule by looking at how water
flow, and Navier-stokes are still open. So from the inside frame it's gonna be hard to model
the 4D medium. I rather have the knowability of the problem clear from the beginning here.
The argument that external reviewers kept on making about #1-gap is ill-formed. A compatible
approximation for the substrate behavior from which the initial constants could be extracted
is still an accomplishement, especially if we only need a small 4-6 of them to derive the 19
of the SM."* Operative form: the EXACT microscopic kernel is inside-frame underdetermined in
principle (the knowability boundary, stated up front, RUL-051 discipline); the V4 success
criterion for §D.5 is a COMPATIBLE EFFECTIVE KERNEL CLASS with a small counted constant set
(target ~4–6) from which the SM's ~19 *(REFINED 2026-08-27, RUL-108: the quark sextet is ONE map on a 3x2 grid — three generations crossed with two isospin types — not six obligations; the object owed is the configuration->mass map, and non-observable does not mean useless. CKM/PMNS stay owed as directly-measured observables.)* derive — parameter compression IS the accomplishment,
and the round-3 reviewer's own criterion concedes it ("if the kernel has a few constants and
reproduces the SM's parameters, then the brute-fact count inverts"). The demand for a full
microscopic derivation is recorded as ill-formed at family level. The gear-locking seed
(`knowledge/candidates/GENERATIONS_GEAR_LOCKING_SEED_2026-08-23.md`) is the named first lever:
mode-locking structure constrains the kernel class from the generations side.

**★ D-1 — DEFERRED, KERNEL-FIRST (2026-08-27, the human coordinator's live grant: *"Follow your
recommendations"*, granting the decision package's leans). THIS IS NOT A NODE — NOTHING IS PICKED
HERE, and the absence of an arrangement row below is the record of that.** The population audit's
heaviest owed decision (`knowledge/audit/v4_population_audit_2026-08-27/V4_POPULATION_AUDIT_2026-08-27.md`
Part 5, D-1, five branches) asked whether V4 enters at V3's regular-D4-at-the-back-fit-size
arrangement, keeps the lattice and moves only the size, moves to a different regular lattice, leaves
the lattice altogether, or defers. **The ruled answer is DEFER — the fifth branch, which the audit
itself records as "a real option and not an evasion".** Operative content, so a later worker cannot
read more or less into it than was granted:
- **No arrangement branch is taken.** V3-1 remains V3's pick; V4 makes none, and no V4 node
  supersedes it. Nothing here re-prices, endorses or retires any of the five branches — they stay on
  the audit's Part-5 sheet exactly as costed, both wounds per branch.
- **The V3-1 GATE (RUL-101/E-4) is UNCHANGED and in force in its narrow form**, including its own
  anti-spread clause: it fences that pick and the dimension-six quotation only, and does **not**
  freeze the kernel programme, the generations arc, the mass sector or the hadron machinery.
- **Kernel-first, and that is the order the grant fixes:** populate V4's decision-free **Tier 0**
  (the block below), then run the **clean-slate kernel programme** (`V4-kernel`) before the
  arrangement is revisited. The audit's own ordering warnings bind meanwhile — do not populate the
  bond layer before the arrangement, and do not populate the hadron layer before PROBE B.
- **RE-OPEN TRIGGER, either limb:** (a) the kernel programme **delivers branch selection** — which is
  the object that would decide it, since N70 puts branch selection fully in the kernel's hands (no
  computed vacuum branch is a tree-level local minimum, so static energetics selects nothing); or
  (b) the kernel programme **stalls**, judged at the **next consolidation review**. Either limb
  returns D-1 to the human with the audit's five branches intact.
- **D-2, D-4 and D-5 remain OWED and unhurried** — the DM-support pick, the mass definition, and
  where V4 should deliberately diverge. Nothing at this pass touches them.

| Node | The pick | Status | Revert |
|---|---|---|---|
| **V4-0 — the spinor CARRIER = `Cl⁺(4,0)`** | The even subalgebra (full body so(4): two body su(2)s), NOT the minimal ideal `S` (one body su(2); the Bell-wing/qubit carrier, R-167). Ground: the S-vs-Cl⁺ orphan closed by R-172's transfer computation; PINNABLE (a second gauged body factor is inside-frame observable in principle). | **SIGNED 2026-08-23 (RUL-098)** | Re-pick `S`: the body-frame route dies on that carrier (one su(2), both halves charged — diagonal-class phenomenology, refuted by the datum); the member must then host weak isospin outside so(4) |
| **V4-ASD — what does the body-ASD copy do?** | Menu: {generation seat (ungauged) ; gauged SU(2)_R (the LR reading — W_R exposure lives HERE, mechanism-gated, prediction-shaped; the gauged reading carries the FCNC-gated degeneracy debt) ; inert}. The ℤ₃-phase escape named: the degeneracy tension fires under the seat reading, not under the meta-time index-set reading. Discriminators: Layer A (cheap) image_dim(R_ASD\|W₊)=0 vs R-076 two-ideal occupancy; Layer B the collective quantization (= the K2 stabiliser computation). **★ LAYER A WAS RUN 2026-08-23 AND IT DID NOT DISCRIMINATE — the located gap, canon §4 form.** *TRIED:* discriminate the menu at Layer A by transporting the body action to R-076's own module `S = Cl·s₀`. *FAILED BECAUSE* (a) the transported action `ρ(h): x·s₀ ↦ x·h·s₀` is **not** left-`Cl`-linear and **fails the canon §5 / RUL-099 invariance test against the BANKED grade-one lock on `S`** (9 of 9 nonzero; 0 of 9 under the grade-two alternative), while on the V4-0-signed carrier `Cl⁺` the same test is **VACUOUS** (`leak(L_K¹, Cl⁺) = √2` — the banked boost is a vector and is not an operator on `Cl⁺` at all); (b) the recorded seat reading **names no module and no action**, so no single-module `j`-content — the right-regular table OR the adjoint counter-table — can reach it, and the flavour-multiplicity space where the question actually lives was never touched (a `j = 1` DOES live in the module: the ASD triple itself under the ADJOINT action, Casimir ratio exactly 8/3 — the round's *"no `j=1` anywhere"* headline dropped its own C-33 action axis and is WITHDRAWN); (c) R-172(5)'s scope fence bars **both** the asset and the cost — `⟨(L_J, ρ(ASD))⟩ = End(S₋) = 16`, but so does the weak-side mirror `⟨(L_J, R_SD)⟩ = End(W₊) = 16`, and an argument form that refutes banked **R-079** when mirrored is invalid at this layer. *WOULD CHANGE IF:* the lock realization is settled on grade-two [**2026-09-22: the grade-two escape Kⱼ = ½e_{j4} is COMPACT — K² = −¼, [K₁, K₂] = +J₃, i.e. so(4) and not so(1,3) — so it makes the I₄ grading invariant by UNDOING C5's Lorentz result; reproduced on the engine, external review §2.1**] (making both the grading and the label observer-invariant) **AND** a passage commits the seat reading to a named carrier and action (local module vs multiplicity space) **AND** S4 is built so module identities may be promoted to species statements at all. **NOTHING NARROWS: the menu reverts THREE-WAY** — `seat` is NOT struck, `seat-as-index-set` is NOT "the only door", `gauged SU(2)_R` gets NO exact fit, and the FCNC-gated debt is neither re-based nor discharged nor confirmed — it is **UNTOUCHED** and stays exactly where keeper K3 put it. **BANKED FROM THE ROUND: only the Φ bridge** (`spinor_module_graded_iso`, CORE) — `Φ: Cl⁺ → S`, `x ↦ x·s₀`, a left-`Cl`-linear bijection with `Φ(W±) = S±`, which closes the **graded-left-module** half of the two-module orphan and licenses the previously silent `W₊/S₊` identification; it closes neither the carrier node nor the `I₄`-grading↔observer-chirality bridge. **EXPOSURE E-1 RIDES HERE:** R-076's `I₄`-graded occupancy is **not boost-invariant** in the banked realization (`e₁P₊ − P₋e₁ = 0` exactly, `L_{e₁}(S₊) ⊆ S₋`), so *"occupies one ideal / occupies both"* is not a Lorentz-stable species property there — PRE-EXISTING, not created by the round, and not previously carried. Records: `knowledge/audit/external_review_2026-08-22/V4ASD_LAYERA_2026-08-23.md` (its §CONSENSUS SUPERSEDES the body) + `VERDICT_REVIEWER_LAYERA_2026-08-23.md` + `JOINT_BANKING_2026-08-23.md`. **Layer B (the K2 stabiliser computation) is the SOLE decider.** | **OPEN — no pick; Layer A ran and did not discriminate** | Node stays as record. Nothing reverts, because nothing was picked and nothing was struck — the Layer-A round's own drafted cell is WITHDRAWN IN FULL and never entered the tree |
| **S4-REAL** (realization: real-space soliton vs Volovik spectral node) | See the S4-REAL node above (RUL-094) — shared with V3. **The spectral half is CLOSED on the banked substrate (PROBE_SPECTRAL_NODE_2026-08-23): the banked D4 frame-bilinear dynamics sits exactly in the real symmetry class (codimension-2 nodal surfaces, ℤ₂ Berry phases only, no finite-k node — the "Chern ≡ 0" figure was WITHDRAWN 2026-08-23 as not robust, the codimension IS the signature; this site missed that annotation until 2026-08-28, the drift class's fourth recurrence), the SM's own node is marginal (N₃ = 0 — momentum-space topology does not protect the SM), the branch would reopen the dim-4 LV channel R-016 closes, and the dynamical payoff requires a filled FERMIONIC sea (Volovik in print) which the substrate does not have. WOULD-CHANGE-IF, re-cut 2026-08-28 (R-191): **DOOR 1 IS OPEN** — G-8, the first concrete kernel candidate, is first-order/precessional and its strict Ω⁻¹H form LEAVES the real class on both branches and both dials (`g8_kernel_kc1_gate`; escape (a)'s first disjunct MEASURED-AND-FIRES); doors 2 (fermionic microstructure) and 3 (a surviving R-016 story) remain against, and firing a NECESSARY condition removes a closer without opening the branch — the Ω₀-free zero-set transfer still blocks it exactly, and sufficiency is KG-2. **A POSITIVE KG-2 (a stable non-Hermitian topological charge in some G-8 dial region) MOVES THIS ROW'S RESOLUTION COLUMN, not just this would-change-if (keeper L-1).** Yield banked from the closure: KC-1, the symmetry-class kernel filter. | real-space soliton STANDS (the spectral alternative probe-closed on two doors + the zero-set transfer; door 1 open, KG-2 live) | as recorded |
| V4-1 … (static/geometric) | Inherited from V3 per the inheritance policy above | inherited | V3's own revert list |
| V4 ← V3-1b (the M-3a inheritance split, pinned at grant time 2026-08-27, RUL-110 — a boundary case recorded early rather than at first use, on the human's V3-vs-V4 question) | **The STRUCTURAL half inherits; the RATIO stays clean.** The two-rate drive-referenced defect rotor + the V3-1a referent that makes the two rates invariant are static/geometric → V4 inherits them with the rest of the V3-1 chain. The **rate ratio** is kernel-routed → V4-kernel CLEAN SLATE, exactly where the inheritance policy's stated motive points (*"the new generation-mechanism with the tongues will give us a good lever"* — the tongues lever now operates on a defect whose two-rate statability arrives free). | inherited (structure) / clean (ratio) | Rides V3-1b's own revert clause, including its automatic coupling to V3-1a |
| **V4-HADRON — the hadron/Skyrme toolbox (D-3, TAKEN 2026-08-27)** | **INHERIT-CONDITIONED.** V4 inherits V3's hadron machinery — the ANW/Skyrme semiclassical toolbox (V3-10, import I-5) together with V3-10a's cell-scale target identification as its carrier — **with THREE ENTRY CONDITIONS WRITTEN INTO THIS NODE, not attached to it later.** **What this pick is and is not: it declares V4's inheritance POSTURE toward the sector; it does NOT populate the hadron layer.** That layer is audit Tier 3, it consumes the bond layer and hence the arrangement, and D-1 is DEFERRED — so under the audit's own ordering warnings (i) and (ii) nothing here is computed, calibrated or entered at this pass. **CONDITION (i) — THE CELL-SCALE TARGET-SPACE TOPOLOGY IS OPEN.** V3-10a is recorded OPEN and the banked usage is the first branch, not a ruling: the menu is three named candidate manifolds — `8×S¹` with `π₃ = 0`, `SU(2)/U(1) = S²` with `π₃ = ℤ`, and the orientation class `S³×S³` with `π₃ = ℤ×ℤ` — **one of which gives no soliton at all**, and `8×S¹` is the branch actually under test. **PROBE A, the topological probe, is UNRUN.** A surviving target of dimension < 3 carries `π₃ = 0` ⇒ no Skyrmion, no hadron sector, and this node's whole content goes with it. Carried latency: pick 9a's target manifold and PROBE A's three-manifold menu both rest on the broken-symmetry manifold of the reference state N70 unseated, so the unscanned reconstruction may present a manifold NOT on A's menu. **CONDITION (ii) — THE TREE-LEVEL SCALE TENSION STANDS AT ~16–18 DECADES, AND THE REFERENCE STATE IS NOT A TREE-LEVEL MINIMUM.** R-189 (PROBE B, banked): the linearised healing lengths run from the measured minimum `ξ/a` = 5.38834 BRANCH body-diagonal / 5.42909 BRANCH axis — a **SAMPLED** minimum, not a derived bound, and the naive `√(12/g)` is the FLOOR not the value — through median ~9.03 / ~9.24 to p95 27.9 / 25.6, diverging on each branch's own phason axis, with the longest tree-level length anywhere in the linearised theory the soft channel's `2π/\|k*\|` = 1093.96 `a` / 321.89 `a`. **The frozen conditional framing travels VERBATIM with every quotation of the gap: a structural tension conditional on two INPUT scales — the grain size is a `G_N` back-fit and `f_π` is fitted — never a derived violation.** And N70: the lowest band has NEGATIVE curvature perpendicular to `k₀` on **BOTH** branches — `−0.02305291` BRANCH body-diagonal (new, non-single-`q`, firing N62's would-change-if (1)) and `−0.27180254` BRANCH axis (reproducing N62's closed form to seven figures, a re-tread) — so **no computed vacuum branch is a tree-level local minimum and branch selection is fully kernel-routed**; the two soft modes fail in **different sectors**, the body-diagonal one 100 % inside the L-orbit triple and the axis one pure `e14`. **Mandatory conditioning that travels with the lengths:** they are short-distance relaxation lengths inside a measured window — the state is a genuine local minimum against every mode of wavelength below 773.2 `a` / 227.0 `a` — and they are **NOT** "healing lengths back to the vacuum", because the state is not one; that wording must not return. N70's own would-change-if (1) is unmet: the from-scratch commensurate-supercell energy evaluation (side ~59) is the condition of banking the body-diagonal leg and is docketed, not claimed. **CONDITION (iii) — THE MARGINAL-QUARTIC TRANSMUTATION ROUTE IS CLOSED, IN THE HONEST FORM.** R-190/N71: **the mechanism applies and does not reach** — never "the mechanism is absent", the first build's wording, refuted by the contra-briefed review and struck. The flow is **one-loop exact** (no power of the quartic couplings at any order in the chiral expansion — stronger than QCD's own β) and **IR-FREE, hence IR-ANCHORED**: its free parameter sits at the **measured** hadronic end, so a hierarchy obtained this way is read off, not generated. The reach is **1.9937 e-folds against the 36.68–42.00 required** across R-189's eight branch- and direction-labelled anchorings — about 5 % of the exponent — and the cell-end coupling that WOULD work is excluded 21× against the banked `e = 5.45`, 95× against the measured ChPT value, and lies ~25 loop-factors outside the domain of the very β that would carry it. **So V4 may not enter this sector expecting its own running to generate the hadron scale: the hierarchy question routes to the §D.5 KERNEL.** The one live escape is **ARGUED AND UNVERIFIED — a pointer, not a result:** hidden local symmetry / vector-meson dominance (the `ρ` saturates `l̄₂`; the HLS gauge coupling is a marginal coupling with a propagator and a quadratic β). **"Earned dimensionful scales" stays at 0** and the Core paper's scale-class row does not move. **MENU:** **inherit-conditioned (THE RULED PICK)** / gate-further — hold the sector out of V4 until PROBE A and a banked energetic pricing are in hand (the audit's Branch B, its own stated preference had it been free to state one) / drop — V4 is a structure candidate and hadrons stay with V3 (Branch C). **GROUND:** the decision package built on the audit's Part-5 D-3 costing, put to the human coordinator and granted live 2026-08-27 (*"Follow your recommendations"* — the session lead's lean was the conditioned middle branch). RUL-048 satisfied in-pass (menu + pick + why + revert); **non-Core** — the hadron sector is instance-level — so it proceeds on this node. **WHAT RIDES IT:** §C.1's hadron chains, R-144's calibration chain, `f_π² = 8J/a`, every hadron number; V3-5's `e_ANW` scheme (DEFER-TO-FIRST-USE, audit rank 22); audit clusters 5, 6 and 7 inherit or fall with it; and cluster 13 (FR / induced topological terms — V3-11's named discharge route) is entry-conditioned on **this node AND D-1**, which is why the fermionic bit inherits at Tier 0 while its retirement does not. | **TAKEN 2026-08-27 — INHERIT-CONDITIONED (the first of the audit's five OWED decisions to be ruled)** | **Un-picking restores D-3 to OWED with the audit's own three branches intact** (A inherit / B gate on the energetic probe / C drop), and this node is struck whole — the three conditions go with it, since they are the node's content and not free-standing claims. **The node FIRES if any of R-189, N70 or R-190 reverts in a way that changes a condition:** R-189/N70's would-change-if (1) — a commensurate-supercell evaluation failing to reproduce the negative curvature — un-conditions condition (ii)'s branch clause; import **I-32**'s revert (refusing the Gasser–Leutwyler running) strikes R-190's rate and e-fold count and leaves N71 standing on leg L-B, which changes condition (iii)'s NUMBERS but not its verdict; **PROBE A returning a target of dimension < 3 discharges this node by removing the sector**, at which point V3-10a's own revert clause governs and the calibration table loses its carrier. Otherwise this node rides **V3-10 and V3-10a's own revert clauses** (I-5's excision row for the toolbox). |
| V4-kernel | **CLEAN SLATE** — no inherited kernel/memory pick; the tongues lever + the front-propagation candidate class (RUL-092's mechanism exploration) are the named starting constraints | OPEN | — |
| **V4-kernel-OP — the G-8 mobility-frame operationalization (registered 2026-08-30, the N1 round's banking-stopper S1 closed the RUL-048 way)** | **MENU, COMPUTED (`n1_opmenu.log`; engine `g8_n1_lock_phase_rotation`): where does the body torque F act?** The pin "F conjugate to the banked H(k)" fixes F, NOT its frame. Three members, all reproducing R-192's L(k) (no linear gate discriminates): **op A** body-torque-LEFT (as N1 ran) — η-equivariance BROKEN (4.8e-2), γ-channel PUMPS energy at η ≳ 1.8, uniform cell beyond c\* gives a one-shot excitable LATCH (Δ ≤ 7e-11); **op B** body-torque-RIGHT (the gradient flow) — equivariance EXACT (1.2e-14), γ strictly dissipative, c zero-work (R-192's own channel designations), uniform cell gives a persistent ρ-capped pattern with a RELATIVE EQUILIBRIUM (R-194): a frozen profile rigidly drifting along B₀ — NOT a limit cycle, the closed orbits are non-isolated gauge translates — and the neutral family is 3-PARAMETER: the gauge 2-torus PLUS the phason (R-195); steady in the co-rotating frame; T = 2π/|ω| ≈ 34.5 tu and the cycle-average Δ = ω/Ω₀ = +0.01301 are ONE measurement (quote the CYCLE-AVERAGE with the PERIOD — never a point value and never a band midpoint, a biased estimator: +48% at d/ρ = 0.57; the once-quoted +1.2947e-2 is ONE CYCLE PHASE; the apparent intra-cycle band is ≥96% detector geometry, physical slip modulation ≲ 1e-5 in Δ; R-194 re-cut); **[measured on CF16 — the BAND-HOSTING cell, long-axis k_min = 0.278 = the drive-fed band peak at this dial; on band-excluding cubic cells the same dial/op has a deep-stable background (−5.78 cell-projected on C8) and no pattern forms — N2R keeper C-5; the number is the BACKGROUND's own slip, never a defect frequency]** **op C** spatial-torque-LEFT — equivariant like B, pattern with a frequency SURPLUS Δ = −1.4009e-2. **THE PICK IS OPEN — escalated to the coordinator with both branches costed** (op A: excitable/neuron-typed vacuum — the latch REAL on the infinite lattice from c\* through c/c\* ≈ 2.7, keeper-corrected map; op B/C: oscillatory vacuum, uniform-cell deficit CYCLES exist beyond c\* — an UNREGISTERED finding of the round: the pattern the prereg's P4 described and the deficit its P5 denied, in flows the prereg did not register, keeper C-3). Measured grounds lean B/C (dissipativity-definiteness); op A defensible as the drive/space-frame placement — a substrate question. **N2 (2026-08-30) RAN A and B: no defect survived in either — the seeded static π₃ texture collapses at every dial (N73), so the survival observable does not discriminate; op C — the one member whose generator is core-localized (F_sp = UFUᵀ, site-dependent exactly at the core) — UNTESTED. The pick stays OPEN with its designated empirical face returned null; the LIVE DISCRIMINATOR CANDIDATES are (i) op C at one shared dial (cheap; step_C exists in n1_opmenu.py) and (ii) ~~the rotating-core round~~ **RETURNED NULL (run N2R, 2026-08-30: on band-excluding cells nothing survives under EITHER op — the second time the defect observable failed to discriminate, both times for the same cell reason). THE REPLACEMENT (N2R keeper O-1): a BAND-HOSTING-cell defect stage (C24 or CF-class) — the first configuration where the two ops give genuinely different backgrounds at one dial (op A: latch-then-quiet; op B: persistent pattern + steady slip), so a seeded defect there is the first DEFECT observable that CAN discriminate the menu; adding op C makes one stage answer (f3)(d), this pick's empirical face, and N73's (op C) handle at once** — **N2D (2026-08-30/31) RAN the band-hosting stage on ops A and B: the survival face returned null a THIRD time (winding dead at 0.25 on the active background too), but the DEFECT-SIDE face is NO LONGER NULL — op A: the defect's history shifts the latch register (η = 0.205 vs the twin's 0.174); op B: the co-evolved element forms a different pattern configuration (polarization overlap 0.14; the once-banked 'slip 0.0084 vs 0.0130 / 31% smaller' WITHDRAWN — a cycle-phase-and-geometry artifact, the cycle-averaged difference is −1.3% — the real datum is TWO-AXIS: the cycle PERIOD relaxes toward the twin's within ~1 cycle while the PERPENDICULAR-PLANE ORIENTATION CONTENT d/ρ 0.570 vs 0.0083 is stationary over 2.6 periods — and R-194 re-tiers BOTH: the period relaxation is transverse relaxation onto the RE family (reproduced by defect-free nulls) and the persistence is an exact zero mode, so NEITHER is a fork signature; attractor-status OPEN; R-194 re-cut). OP C TESTED on a band-hosting DEFECT stage at N4 Track 1a (R-198; six elements, three ρ, two c): no discrimination on the two verdict observables (certificate death 0.25 at snapshot cadence; near/far contrast within 0.02) — the survival face returns null a FOURTH time — but the DEFECT-SIDE face is NON-NULL for the first time: |F|max at the first snapshot is systematically LOWER under op C at 6 of 6 defect elements (C/B = 0.463–0.911; up to 79 % over the trace) against ≤ 1.03e-3 on the defect-free references integrated in the same stages (amplification 1.6e3–7.0e3×). The 0.005-tu transient WAS then run for op C (`n4a_analysis_transient_opC`): certificate at 0.100 / 0.060 / 0.015 / 0.005 vs op B's 0.105 / 0.070 / 0.020 / 0.005, core strain at t = 0.005 63.6 vs 74.8 (k = 4) and 32.0 vs 22.9 (k = 31) — same fate, slightly sooner, different transient. THE PICK STAYS OPEN; discrimination is not selection. **op B′ — the mobility-ROTATING flow `V = (γ_d − c·ad_B₀)⁻¹ γ_eff F` — was registered here at the Track 2 recon and REMOVED at R-199 (keeper N4b, computed): it does NOT reproduce L(k) (reactive boundary c* = 68.16 vs 8.479 at Ω₀ = 14; +0.3847 vs −4e-6 at c = 12; identical only at c = 0, `n4b_keeper_linear2.py`) and is therefore not a placement of this kernel but a DIFFERENT kernel — it lives under V4-m5 as the overdamped limit of the Coriolis-placed inertial form. This menu has THREE members, none selected.**. Stated explicitly (N2D keeper): DISCRIMINATION IS NOT SELECTION — the pick stays OPEN and no criterion has been offered; and the N1 keeper's L-3 caution carries — a pick decided by a defect observable must name that observable class, while every op-scoped row is written from uniform-cell observables** — and note the N1 keeper's L-3: if op C behaves differently on a defect, the pick would be decided by a DEFECT observable while every op-scoped N1 row is written from UNIFORM-CELL observables (name the observable class when picking).** RELATION TO V3-9 (keeper item 9, the orphan closed): V3-9 pins the kernel BRANCH as 'driven-hysteretic' while every banked kernel-programme result (R-191/192/193) computes with the saturating-ceiling realization Γ_sat and consumes no hysteretic pick — and R-193 records that the §D.5.3 rich/hysteretic-vs-fading fork is NOT evidenced by the latch (a marginal coordinate; no bistability observed) and stays open. | **OPEN — no pick; N1 banked with every dynamical row op-scoped** | Whichever member is picked-against fires the re-scope of N1's op-scoped rows ONLY (R-193's exact identities — the mobility rotation, the slow-block latch-invariance — are member-independent operator facts and survive as facts about op A's flow); a pick FOR a member promotes that member's column to the round's headline and demotes the others to menu records |
| **V4-m5 — the τ₅ INERTIA (opened 2026-09-02, RUL-124, the coordinator's N4 decision D2)** | **CANDIDATE dial, clean-slate (not inherited).** A second-order term `m₅ U_ττ` in the kernel's own evolution parameter (its identification with τ₅ — the "hyperbolic face" reading — an OPEN named map, below), so that the left gauge torus carries conserved Noether charges in the γ_d → 0, Ω₀ = 0 limit (NOT "the drive's U(1)": with the drive on the charge drifts 37 % — R-199). **It does NOT reach R-142's object (R-200, keeper N4C collision 3): with a unit-rotor field the charge inertia is Θ = m₅Na²·I₂ for the whole symmetry torus, set by the cell and independent of configuration, so R-142's Θ(λ) premise fails identically here (E2′) — the node is a measured lever against N73 (f2), not a realization of it.** The charge-carrying rotation is the LEFT symmetry of E (R-194's gauge torus, R-193's Goldstone), under which the body-frame velocity is invariant; right multiplication is a descent direction, not a symmetry (dE = −7.69). **And m₅ is not an independent dial at Ω₀ = 0 (meta-observer N4C, bit-exact): (m₅, c, γ_d, Ω₀, V, t) → (1, c/√m₅, γ_d/√m₅, √m₅Ω₀, √m₅V, t/√m₅) is an exact symmetry of the dynamics — the c = 0 family is governed by the single reduced damping γ̂ = γ_d/√m₅, with the gradient flow as its own overdamped endpoint.** **e₅ LAYER FENCE (binding on every consumer):** τ₅ is PHASE / META-TIME; the inertial term is the timelike-evolution role the e₅ litmus permits and NOTHING here makes e₅ spatial — a construction that would is FRAMING, not physics. Menu that stays open with the node: the value and scaling of m₅ (Land's c₅ vs c_meta, both unpinned); whether the first-order flow is the m₅ → 0 limit or a distinct member. Consumers: Phase N4 Track 2 (`g8_n4_*` rounds) and nothing banked. **REALIZATION (R-199, the Track 2 round):** `dU/dt = Ω₀B₀U + U(V − Ω₀B₀)`, `m₅ dV/dt = γ_eff F + c[B₀,V] − γ_d V` — the CORIOLIS placement of the reactive term, which is the Euler–Poincaré equation of the left-invariant Lagrangian `L = Σ[(m₅/2)⟨V,V⟩ + c⟨B₀,V⟩] − E(U)` (variational; body momentum m₅V + cB₀); the FORCE placement `m₅V̇ = G(U) − γ_dV` is REJECTED (not Lyapunov at c ≠ 0: dE_tot/dt ∋ c⟨V,[B₀,F]⟩, recon leg 3). Its overdamped limit is **op B′** (moved here from V4-kernel-OP: not a placement, a different kernel — it does not reproduce L(k), c* 8.479 → 68.16 at Ω₀ = 14). **Conserved charge — a DRIVE-OFF object (reviewer re-cut 11, computed: Q_can drifts 37.15 % dt-independently at Ω₀ = 14 against 9.4e-9 at Ω₀ = 0; the node's opening sentence "the drive's U(1) carries a conserved Noether charge" is REFUTED as written — the charge lives where the substrate's drivenness is switched off, Ω₀ = 0, and there with γ_d = 0 the system is conservative; the decay law at γ_d > 0 is dQ_can/dt = −(γ_d/m₅)Q_kin, verified to 1.7e-5; an engaged ρ_sat breaks it at O(1/ρ²): Noether residual 8.1e-8 at ρ = 1e6 → 46.1 at ρ = 42)** (canonical, Ω₀ = 0, γ_d = 0): `Q_can = m₅Σ⟨A_s,V_s⟩ + cΣ⟨B₀,A_s⟩`, A_s = UᵀB₀uU — drift 1.9e-8, second order in dt; the kinetic half alone drifts 31 %. **The left symmetry is the full 2-torus {B₀, ⋆B₀} = R-194's gauge torus, with TWO exact canonical charges** (measured, R-199 post-hoc: Q_can(⋆B₀) = −366.46, drift 6.8e-9 → 1.8e-9 relative at dt → dt/2, beside Q_can(B₀)'s 1.0e-10 → 2.6e-11). **LC-2:** an engaged ρ_sat breaks the charge's Noether identity at O(1/ρ²) — the ceiling lever and the charge lever are mutually exclusive in this realization. **THE γ_d CONSTANT (keeper item 12):** γ_d is the friction on V; the overdamped limit with γ_d ≡ γ (= 1, the time unit) is op B′; the γ_d = 0.1 arms are a SECOND new constant of this node (RUL-048 budget clause — counted, not free), not a re-scaling of γ. **THE e₅ FENCE, re-cut (keeper LC-1):** m₅ multiplies the second derivative in the G-8 kernel's OWN evolution parameter; the identification of that parameter with τ₅ — and hence the "second-order-in-meta-time / R-112 hyperbolic face / Land action" reading — is an OPEN NAMED MAP (RUL-030 class 2), assembly-recorded here, at EOM-map L2-1 and at the worklist's SHP inside-face programme stage (1); nothing here makes e₅ spatial, and nothing here banks the identification either. **PRIOR ART (meta-observer F3, R-199; credits, not imports — no result consumes them):** op B′ IS the Landau–Lifshitz form of a Gilbert-form damping (Landau–Lifshitz 1935; Gilbert 2004, doi:10.1109/tmag.2004.836740) — the mobility rotation by the reactive channel; the freeze's own 1 + c²/γ_d² dilation factor is Kikuchi's α/(1 + α²) (1956, doi:10.1063/1.1722262); the Coriolis-placed second-order form is the inertial LLG (Ciornei–Rubí–Wegrowe 2011, doi:10.1103/physrevb.83.020410); and "survives longer but never plateaus" is the OSCILLON (Gleiser 1994; Copeland–Gleiser–Müller 1995), whose standard statement predicts this round's result in advance. The delta claimed here is the exhibition on this substrate with the exact placement clauses and the no-barrier charge result, not the forms. **BRANCH B (RUL-125, 2026-09-04): D1 ruled — one clean-up round on this node (`knowledge/audit/g8_n4_track2b_2026-09-04/`: the fixed-N arm at c = 0, γ_d ∈ {1, 0.5, 0}; the hot-seed elements at K(0) = {1, 3}×|E_el|; the HOLDS-LOWER criterion), then Track 1b. Pre-freeze exact statements on this realization: E1 — dQ/dt = −(γ_d/m₅)Q against dE_tot/dt = −(2γ_d/m₅)K, so no dissipative rung is fixed-N where it is cold; E2 — at c = 0, K_min(Q) = Q²/(2m₅Na²) is configuration-independent (|UᵀB₀uU| = |B₀u| site-wise): the static Q-ball barrier is exactly zero for every U (conditioning class: c = 0, this bi-invariant kinetic metric, this realization); the Q-ball mechanism needs a U-dependent inertia, which an amplitude field (Track 1b) has and a unit rotor does not.** **BRANCH B OUTCOME — R-200 (2026-09-04): N73 (f2) EXCLUDED BY CONSTRUCTION at c = 0 on this realization (E2′, exhaustive over both charges); the dynamical arm a complete monotone γ̂-sweep, negative at every decided element (certified-degree-0 energies 9.2–31.1 % vs the gradient flow's 1.80 %); the exactly-fixed-N arm undecidable by the criterion; the amplitude would-change-if quantified at 0.255 % of |E_el| at Q = 416.5. Track 1b next (RUL-125).** | OPEN — CANDIDATE, one constant against RUL-098's 4–6 | Strike the node: Track 2 results → FRAMING; constant count −2 (m₅ and γ_d); N73 (f2) returns to "awaits the second-order route" (RUL-124's revert list) |
| **V4-TIME — which time is the flow's? (PROPOSED 2026-09-09 at GS-1's ceremony; NOT PICKED — a CORE-touching pick, RUL-048 (iii): the coordinator's plain-language sign-off OWED before any result rides it)** | **MENU (three), written at the keeper's collision C-1:** (i) **the real-k₄ lock** — R-123's banked text: a rotor on the lock is x₄-periodic at k₄ = ω/c_meta, REAL; (ii) **the Wick rotation t = i x₄** — GS-1's combined face (the flow's time and the e₄ advance one variable, k₄ = iω), which GRANTS the Euclidean→Lorentzian step the D4 note §8.5 calls 'the hard part … not done here' and under which the ⋆B₀ channel is lightlike exactly through quartic order at μ = 0 (η⁽⁴⁾ = 0 in that channel) while the two slow-block polarizations split by Δc²/c² = −|k₀|² (3.4 %); (iii) **two independent times** — the inertial-m₅ face with the e₄ symbol at k₄ = 0 (Track 2's V4-m5 dial; recon 2's −1/12 and the 0.57 % split). Costed: (ii) buys the cleanest E1 reading in one channel and the sharpest dimension-four exposure (I-34); (i) leaves E1 at the inertial-face reading; (iii) is the banked candidate as run. GS-1's exact μ-family is a statement about the SYMBOL — DERIVED-GENERIC-given-equal-weight-D4 — whichever branch is picked. | PROPOSED — no result rides it; DEPENDENTS (recorded 2026-09-09 per RUL-048(iii), keeper R-205 item 13): the GR-1 world-tube note's carrier-phase premises (A)–(C) consume this pick — no GR-1 track may make it by default; UNDECIDABLE-WITHOUT-COORDINATOR (keeper GS-1 C-1) | Strike the node: every 'the framework's own face' sentence reverts to 'a candidate convention'; the I-34 pricing stays conditional on §B.5.4; GS-1 §1 keeps its tier as a symbol fact |
| **V4-ACTIVE — THE ACTIVE-WINDOW KERNEL, Track 1b (opened 2026-09-04, RUL-125 / RUL-126, the coordinator's N4 decision D1 as re-ruled after Branch B)** | **CANDIDATE pick OUTSIDE the banked kernel class — an H9 CHALLENGE, not a family member.** The banked first-order op-B kernel with an explicit NONLINEAR-GAIN WINDOW in its dissipative coefficient, γ_eff(|F|) = γ(1 − a x + b x²), x = |F|²/ρ₁², with γ_eff < 0 in an amplitude window (net linear loss at |F| → 0, gain at intermediate strain, quintic-type loss above) — a gain window in the mobility of a first-order rotor flow (the 'literal cubic–quintic CGL medium' description was WITHDRAWN at R-201; the Akhmediev–Ankiewicz integral balance is the criterion it hosts). **H9 (passivity, Im χ ≥ 0, DERIVED) STAYS BANKED for the family:** this node says the vacuum is LOCALLY an active medium that can pump a defect, which the family holds the opposite of; an object found here is a CANDIDATE of a kernel outside the banked class and is never evidence against H9. MENU: {passive (banked; the R-198 ceiling is the a = 0 limiter), active window (this node)}. Parameterized by what the window DOES (coordinator-directed): centre |F|_c and half-width w in units of the seed's measured |F|₀ = 58.5 (a RUN-BUILDER constant — the DEFECTIVE min-image radius truncated the seed, RUL-128 at R-202; the seam-free build reads 41.66 on every cell), depth DERIVED from the width (RECORD 1). Not the amplitude-field route of R-200's would-change-if (a unit rotor with a window has no amplitude degree of freedom — that handle stays separate and unopened). No m₅ (first order; RUL-124's node unused). Records: `knowledge/audit/g8_n4_track1b_2026-09-04/`. | OPEN — CANDIDATE; THREE window dials — centre, width, and the reference strength ρ₁ (a NAMED constant, 58.517307, since R-203; the seed-relative convention was the RUL-128 artefact) — the depth DERIVED from the width (RECORD 1: −γ/3, −γ/3, −γ/24 — the frozen −γ/2 was over-determined), both registered; the counting clause FIRES at R-201 (a positive statement rides them: a balanced localized state exists at W1) — two dials against RUL-098's budget while the node stands. **OUTCOME (R-201, 2026-09-06): NO EXISTS under the frozen criterion (its localization guard is not invariant under the flow's banked symmetry group and could not pass on this object class); at Ω₀ = 0 / W1 a strain-localized, certificate-silent, gain/loss-BALANCED state (97.6 → 99.5 % cancellation; holds to 16 tu; destroyed in 0.15 tu by the passive kernel) rotating along R-194's left torus at |ω| = 0.26 rad/tu — a CANDIDATE localized dissipative structure, NOT an EXISTS, its co-moving solve owed. THE H9 FENCE, LOUDER: the core PUMPS (γ_eff = −0.0068 on 14 sites); the object exists only by the passivity violation this node licenses and is evidence about a kernel outside the banked class, never against H9.** **OUTCOME (R-202, 2026-09-06): the same object REPRODUCED and CONTINUED on CFW16 — a damped breather (0.451 tu, decay 0.242/tu; the co-moving spectrum confirms it as the leading damped linear mode, −0.266 ± 13.87 i /tu, with no growing mode among the leading four at the pass-2 iterate — R-202 RECORD 6, 2026-09-07) on a pinned non-winding core (at the r_core 2.4 seed; at 3.4 the hosted radius 2.18 is above the representability window and the degree undetermined by instrument — R-205; PICKED 2026-09-10 (RUL-131; the coordinator's plain-language sign-off: 'Approved. Proceed …') — a third V4-ACTIVE menu member, a DEFECT-DRESSED (strain-gated) coefficient: a correction gated on |F| < 1 with admissible coefficient −0.9 to −1.0 removes 87–99.9 % of the hosted states' residual mean drift where no constant-coefficient bond invariant reaches 5 % (K-INV step 1 REV 2); CORE-touching, signed off; the RECON (K-INV-3, 2026-09-10) found the 87–99.9 % figure to be the two-column freeze-the-bulk move — the one-column member removes 24–31 % with a state-dependent coefficient, raises the core residual, and makes the small-strain medium exactly marginal at c_g = −1 — so the first instance is NOT frozen pending the coordinator's re-read; revert: strike the pick, the node returns to PROPOSED), RELAXING toward an endpoint not yet identified (R-201's residual and |ω| were snapshots; not a member of the two-sided torus family at the Newton floors reached — R-204, D9 open), sitting above the window's lower root by the box's loss in closed form (`g8_track1b2_root_offset_balance_identity`); the two-cell box test UNDECIDED-BY-INSTRUMENT (the min-image defect, RUL-128; two different objects on two differently truncated seeds); an EXISTS under the frozen criterion on one cell, CANDIDATE. THE L1-LATENT TRIGGER: any consumption of R-202 outside the V4-ACTIVE conditioning is a CORE-touching pick requiring RUL-048(iii) sign-off before the pick. Next: the wide cell with the exact seed and the site-matched implant (coordinator-approved 2026-09-06).** **OUTCOME (R-203, 2026-09-07): on the exact seed under one kernel the STRAIN CORE is not box-supported along row 2 at 2× on the cell-neutral measures, while the TWISTED state and its breather were box-supported at image spacing 5.66 at round 3's stopping time (weakened by the continuation — box-dependent in timing, not in existence; RECORD 4's hedge, restored at R-204); the far field is image-sustained; the implant is exact only as a tiling.** **OUTCOME (R-204, 2026-09-08): the same 14-site core on a third cell (row 3 doubled, the 5.66 axis held) — three cells, two independent doublings, one core to ≤ 0.5 % on the cell-neutral measures; the frozen clause NEGATIVE on both fresh seeds as a measurement of NON-STATIONARITY (still relaxing at t = 12; the continued object stationary with its breather at the banked rate); the new datum a discrete core-reorientation / eruption event on every element (the CQ-CGL erupting class, I-33); the twist re-referenced to the core↔halo boundary bond, its un-twist event on every cell with box-dependent timing and an ordering that inverts inside the window (P1 MIXED — no single-time two-cell read measures it); Q2 / P1a / Q4 void as frozen; D9 open; the fresh seeds graze the RP³ rail — the LATTICE REFINEMENT is next and can moot every box sentence in the arc, then ρ₁ as a dial. H9 unchanged: an object here is a CANDIDATE on a kernel outside the banked class and never evidence against H9.** | Strike the node: Track 1b results → FRAMING; H9 unchanged (it never moved); the window dials leave the budget; N73 (f3) returns to 'the class needs an active medium — untested' |

| **V4-0-BRIDGE — the CARRIER EXPANSION the chirality bridge would cost (opened 2026-09-22, the external review v3 apply pass; NOT PICKED — routed to the coordinator)** | Menu, computed on the engine: of the four candidate carriers only the **full `Cl(4,0)`** is invariant under the two-sided realization `γ̂^μψ = γ^μψe₄` that builds the `I₄`-grading ↔ observer-chirality bridge; `Cl⁺(4,0)` (V4-0's signed carrier) is **not**, the reviewer's `Cl⁺(1,3)` alternative is REFUTED, and the Dirac ideal `S` is not. So repairing the chirality arc costs V4-0. **The five owed checks this branch carries** (from the review's item 2, recorded here so assembly finds them): (1) the CARRIER — **ANSWERED**: only the full `Cl(4,0)`; (2) the two-sided COMPLEX UNIT against C14 and the Born package — open; (3) a MASS MECHANISM without a fundamental scalar — **computed**: the space of operators commuting with the host and anticommuting with the chirality has dimension **zero**, so any half-connecting mass term breaks the weak symmetry, and the honest form is *no fundamental scalar field, but a background transforming under the host*; the next computation is the host's adjoint decomposition of `{M : {M, Γ} = 0}`, which specifies what that background must carry; (4) the HYPERCHARGE content of the commutant — open, and the commutant *contains* room for it, never *yields* it; (5) the role of the ASD half under the two-sided realization — open, and it collides with V4-ASD. | **NOT PICKED — CORE-touching; the coordinator's word is owed (RUL-048 commitment budget)** | If the coordinator takes the expansion, V4-0 reverts and every site reading `Cl⁺(4,0)` as the carrier is re-read (R-167, R-172, companion R-060/R-079, §B.3.1); if the expansion is declined, the chirality bridge stays UNBUILT and BRIDGE-CHIR conditions the weak arc permanently |
| **V4-ACTIVE-REQ — STABILITY and PASSIVITY are two separate requirements (recorded 2026-09-22, the external review v3 apply pass, its item 9; a REQUIREMENT record, not a pick)** | On an ACTIVE substrate the two come apart and the corpus must not read one off the other: **passivity** (no energy is extracted from the vacuum by an arbitrary probe cycle — an Im χ sign condition, the property a passive kernel has by construction) and **STABILITY** (no linear mode of the state grows — the property an active state must be shown to have). The active window is entered precisely by giving up passivity, so every stability statement there is **owed a computation** and is never inherited from the passive class; equally, a measured stability is not passivity restored. | standing requirement | Fires if a future kernel is shown passive: the two collapse only there |

### THE TIER-0 POPULATION — the decision-free rows (populated 2026-08-27, the human coordinator's live grant)

**What this block is.** The V4 population audit (`knowledge/audit/v4_population_audit_2026-08-27/V4_POPULATION_AUDIT_2026-08-27.md`,
Part 4) orders V4's population by dependency and puts **eight** items in **TIER 0 — takes nothing and
blocks nothing**. Those eight are populated here, **and only those eight**, because the audit's own
three ordering warnings bind: (i) do not populate the bond layer before the arrangement; (ii) do not
populate the hadron layer before PROBE B; (iii) do not read V3-11's Tier-0 placement as covering its
discharge route.

**THE GROUND IS THE SAME THREE-PART GROUND FOR EVERY ROW IN THIS BLOCK, and each row names any
addition to it:** RUL-098(4)'s **static/geometric inherit-default** (*"static/geometric picks → V4
inherits unchanged unless a V4 node supersedes them"*) + **the audit's INHERIT verdict** for that
feature at its named rank + **the human coordinator's live grant of 2026-08-27** (*"Follow your
recommendations"*, granting the decision package's leans).
**THE REVERT CLAUSE IS ALSO THE SAME FOR EVERY ROW, and it adds nothing of its own: an inherit-row
reverts if its V3 (or Core / family-input) source reverts.** Each row rides the source node's own
revert clause; nothing in this block creates a new revert obligation, and nothing in it is a fresh
commitment — an inherit is the *absence* of a divergence, which is why these eight were decision-free
in the first place.

**Two roster notes, so the block is not read as more than it is.**
**(1) V3-1b is NOT Tier 0.** The audit ranks the two-rate drive-referenced rotor **11, in Tier 1**,
riding V3-1a and hence V3-1 — so the inheritance split already pinned at RUL-110 (structural half
inherits, rate ratio clean) stands exactly as its own row records it and is **NOT populated at this
pass**, because **D-1 is DEFERRED**. Its row below is unchanged.
**(2) Rows 1 and 8 are held by MEMBERSHIP and by CLASS, not by the RUL-098(4) inheritance of a
*pick*.** A member that diverges at the axioms is a different family, not a different candidate
(Core §4.4), and the family inputs are carried by every member by construction (RUL-096). They are
listed because the commitment count must be honest from entry, not because anything was chosen.

| Row | Inherited content | Ground (beyond the block's shared three-part ground) | What rides it | Revert |
|---|---|---|---|---|
| **V4-T0-1** | **THE CORE AXIOMS AND THE FIELD-AS-ONTOLOGY REFUSAL** — S1a, S2, S3, S4, S5, B-6, whole and unmodified. **Carried scope:** B-6 (preferred foliation = the CMB rest frame) is **the family's kill condition**, a real stated family-wide falsifier, and it travels with the inheritance | Audit **rank 1**, INHERIT **by membership** — not by RUL-098(4), which governs picks. This is the root | The entire kinematic and quantum spine and nothing else: R-001, R-003, R-004 (matter as defect), R-013…R-015, R-018/R-019, R-020…R-029, R-032…R-035, R-043/R-044 | **No revert is available inside the family** — divergence here exits the family rather than reverting a pick. The two booked scopes travel: the signature *placement* on `e₅` is half-chosen (posit + theorem), and the Tsirelson/Bell wing FAILS the separator |
| **V4-T0-2** | **THE CHARGE SECTOR — WHOLE AND UNMODIFIED.** The **discreteness** (`π₃(S³) = ℤ`) and the **`c`-free neutrality identity** (`Q_p + Q_e = 0` for every normalization `c`) derived end to end; the **15-value assignment** riding P4–P7 plus the entered anchor, with `charge_sector_provenance` marking the executable boundary. **Quoted whole, per the comparative ledger's own fence that the two numbers never travel apart: on STRUCTURAL excess two things pass and both of them are this one — the charge arc's two halves and the `B − L` closure; on EMPIRICAL excess the score is ZERO OF TEN.** Risk carried at entry: **AC-40**, adjudicated APPLY 2026-08-27 — the neutron-charge discriminator is not independent of BRESSI at its tightest and survives on BAUMANN 88 at 68 % CL | Audit **rank 2**, INHERIT whole and without modification. No kernel object anywhere in the chain; consumes only the Core state space and the family inputs | The charge arc; `B − L`; GMN's `c = 1/2` as a **derived consistency check, never an input** (canon §1 anti-circularity — charge comes from the winding) | Rides the FAMILY INPUTS rows for the charge anchor and P4–P7; each premise's own would-change-if lives at `charge_sector_provenance`. Refuting any one re-opens the **value table**, never the discreteness |
| **V4-T0-3** | **THE KINEMATICS AND QUANTUM PACKAGE** — the R-013…R-035 class: the Dirac relations and the signature shadow, the Lorentz generators and Thomas precession, the Born subspace, self-adjointness, the Born rule, free Schrödinger, spin-statistics, the Dirac equation, `ρ_A = ½𝟙`, Maxwell, no-monopole, Coulomb, the photon. **TWO HONEST SCOPES TRAVEL WITH IT AND MUST NOT BE DROPPED:** the **signature PLACEMENT** on `e₅` is a **POSIT** (what is DERIVED-A is `Cl(4,0) ≅ Cl(1,3)`; graded HALF-CHOSEN, and canon §7 rules the relabel not to be re-litigated); and the **Bell wing is FRAMING** — `tsirelson_S` rides the un-constructed tensor product (N53) and the singlet, and FAILS the separator outright, so it is weaker than the rest of the package | Audit **rank 3**, INHERIT. Consumes the axioms and the algebra only | Every downstream use of the quantum spine; the observer-side machinery the whole framework reads its data through | Rides the root (V4-T0-1). The two scopes are carried conditioning, not revertible content |
| **V4-T0-4** | **LS-6 — the local state's size (six real parameters).** Inherited as the **AXIOM**, which RUL-083 made **WITNESS-FREE at family level** | Audit **rank 4**. Coherence-decided on four named grounds (keeper record `knowledge/audit/pivot_2026-08-17/KEEPER_SPIN34_ADJUDICATION_2026-08-18.md`) | R-102 (the grain's 4D-orientation local state), R-002 (`π₃` of the orientation class = `ℤ×ℤ`), `N_eff = 6` in the Sakharov chain (R-037), every Layer-1 carrier argument | Rides the LS-6 Core node's own row. **CARRIED DUTY, NOT DISCHARGED HERE: V4 owes its OWN witness on whatever grain it picks** (RUL-083 — *"each family member re-witnesses on its own grain"*), which is audit **rank 17, a Tier-2 duty**. If V4 diverges at V3-1 the witness must be rebuilt; the axiom does not move |
| **V4-T0-5** | **THE WEAK HOST — `weak = SD`, WITH A-P2′ ATTACHED.** Weak isospin on the SD chiral factor, with **side and module named** (A-P2′, RUL-091, after N63 retired the left/space-frame reading — the left centralizer of the lock is a U(1), never an `su(2)`). **Carried and never dropped: A-P2′ is ENDORSED (RUL-084) — a preferred direction, not an axiom — and the entered datum that closes R-171's menu, that right-handed fermions are weak-isospin singlets, is a FAMILY INPUT whose energy scope and would-change-if live in the FAMILY INPUTS register row above.** **A second scope is carried STATED rather than silently: R-171's closure classifies 3-dimensional hosts and is SILENT about a 6-dimensional two-scale one** — an under-claim recorded against us at the V4-ASD revert cell, and it matters here precisely because V4-ASD is open | Audit **rank 5**, INHERIT — *"carry A-P2′ with it, and carry R-171's 6-dimensional silence as a stated scope, not silently"*. The menu is **computed closed** (R-171, DERIVED-A), so this is not a pick V4 re-takes | R-079 and its consumers R-058, R-060, R-061, R-062, R-077, R-082 — V−A, generation-blindness, the doublet and up = SD, all DERIVED-given-it; R-172's body-frame side computation; wins-ledger W-7 (quote it with A-P2 or do not quote it) | **RUL-082's revert clause:** re-stamping A-P2 away re-opens R-171's closure and returns V−A, generation-blindness, the doublet and up = SD to DERIVED-given-an-INPUT-bit. A re-rule to LEFT fires N63/RUL-091's separate revert list. The datum's own reversal is the FAMILY INPUTS row's |
| **V4-T0-6** | **THE GENERATION COUNT — `dim Λ²₋(ℝ⁴) = 3` (E8, B-7). THE COUNT ONLY.** **What is NOT inherited: the seat-vs-index-set reading**, which is deferred to **V4-ASD** — OPEN, with Layer A run 2026-08-23 and **not discriminating** (the menu reverts three-way) and Layer B, the K2 stabiliser computation, the sole decider. The count **names no module and no side**, which is exactly why V4-ASD could not orphan it. **Carried scope:** graded **PASSES-AS-GENERIC** — real, but generic-given-4D, not substrate-specific (canon §5's derived-vs-generic test) — and the **dynamical selection is open (N13)** | Audit **rank 6**, INHERIT the count / DEFER the reading. Every Tier-0 row touching the generation sector is written so that it does not presuppose a V4-ASD branch | R-071 (the count), R-098 (the ASD triple, DERIVED-A); Horwitz's published adjoint-construction reason is the credited prior art | Rides B-7 / R-071 / R-098. **Nothing here pre-commits V4-ASD in any direction**, and when a pick IS taken there, that node's own same-pass relocation duty governs |
| **V4-T0-7** | **FERMIONIC QUANTIZATION — the Finkelstein–Rubinstein scheme (V3-11, import I-20).** An **INPUT** (a menu pick, not substrate-derived), **PINNED**, costing **one bit**, and **branch-blind w.r.t. LS-ℤ₂** — contingent-in-mathematics (both quantization sectors are internally coherent) and over-determined-in-data (the even/bosonic sector has no exclusion principle, hence no shell structure and no stable bulk matter). The **bit-accounting symmetry duty (RUL-063)** travels: bit-sized inputs are counted for every framework in the reference class or for none | Audit **rank 7**, INHERIT. **★ ORDERING WARNING (iii) IS WRITTEN INTO THIS ROW: the NODE is Tier 0 and its NAMED DISCHARGE ROUTE IS NOT.** The substrate-induced Wess–Zumino-class term (the I-1 D'Hoker–Farhi class) that would FORCE the odd sector and retire the bit is audit **cluster 13, Tier 3**, entry-conditioned on **D-1 and D-3** and built on the lattice and the hadron sector. **So the bit inherits and its retirement does not arrive with it** | R-025, R-131, R-136, R-141, R-142 | **I-20's own excision row.** The discharge route reverts separately, at its own node, and is not covered by this inheritance |
| **V4-T0-8** | **THE FOUR FAMILY INPUTS, carried by class (RUL-096)** — the right-handed-singlet datum, the charge anchor (`Q_p = 1`), the four structural assignment premises P4–P7, and `m = E₀` (the soliton-mass identification, vacuum-subtracted, `v = 0` reading). Each is counted; each carries its own would-change-if in the FAMILY INPUTS table above; `m = E₀` is **computed untouched by the M-3a grant** (RUL-110's own "what it does not move" list) | Audit **rank 8**. **Carried by class, not chosen** — listed so the commitment count is honest from entry, which is the whole reason the audit ranks them at all | Named in each input's own row above: the 15-value charge assignment, the A-P2′ chain's second condition, §A.4's mass mechanism and the soliton mass floor under the hadron sector | **Each input's own would-change-if row**, unchanged. Nothing in this block strengthens or re-bases any of them |
